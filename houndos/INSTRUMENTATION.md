# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity go-rest-api start -->
## Go-rest-api

Generated by HoundOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 5 generation passes — ⚠ passed with warnings.
<details><summary>13 issues found and fixed during self-review</summary>

- qr-metric-naming (`cmd/telemetry.go`): The counter name "http.server.request.outcomes" uses a plural noun for a plain count; the repo's other counters mix conventions (auth.attempts, flow.entries). Prefer an unsuffixed singular-count style like http.server.request.count or http.server.requests to keep one consistent convention.
- qr-preserves-behavior (`cmd/otel_middleware.go`): authOutcomeMiddleware wraps the ResponseWriter in a statusRecorder and passes it downstream, but the JWT validator and handlers now receive statusRecorder rather than the original writer; while it forwards Flush/Hijack/ReadFrom, the double-wrapping (httpTelemetryMiddleware also wraps) changes the concrete writer type seen by handlers. This is compile-safe but note it stacks two recorders on protected routes.
- sli-not-implemented (`cmd/otel_middleware.go`): The SLI go-rest-api-http-saturation depends on http.server.active_requests and http.server.worker_pool.size, but the worker-pool denominator uses maxWorkers() defaulting to GOMAXPROCS/MAX_WORKERS, which is not the actual HTTP server worker pool size — the repo's StartServer has no such worker pool. The saturation ratio measures goroutine count against an unrelated CPU count and cannot represent true worker-pool saturation.
- qr-metric-naming (`cmd/telemetry.go`): The counter name "http.server.requests.by_tenant" encodes the breakdown dimension (tenant) into the metric name rather than expressing it as an attribute; the tenant.id is already recorded as an attribute, making the name redundant and off-convention.
- qr-preserves-behavior (`cmd/otel_middleware.go`): authOutcomeMiddleware reads the status via `w.(*statusRecorder)`, but by the time the auth middleware runs, the ResponseWriter has been passed through otelchi.Middleware and SimpleCORSMiddleware which may wrap it in their own writers, so the type assertion typically fails and the fallback recorder is installed anew — meaning httpTelemetryMiddleware's recorder (installed upstream) never sees the denied status either. Both recorders can end up wrapping different layers, and the outer recorder never observes the 401/403 written to the inner one.
- general (`cmd/otel_middleware.go`): httpTelemetryMiddleware is registered before SimpleCORSMiddleware and the auth middleware, so its statusRecorder is wrapped by downstream writers; the outcome/status it records reflects only what reaches its own recorder. Since CORS/auth may write responses via a re-wrapped writer, the recorded http.response.status_code can be inaccurate for rejected requests.
- qr-metric-naming (`cmd/telemetry.go`): New metric name `http.server.request` (an outcome counter) collides conceptually with the semconv histogram base name and is ambiguous — a plain request count is conventionally `http.server.request.count` style or a distinct namespace; using the bare `http.server.request` overlaps the histogram's metric family name.
- general (`go.mod`): The require block pins go.opentelemetry.io/otel and related modules at v1.36.0 while proposedDependencies and the go.mod change hunk introduce v1.35.0 — the two edit hunks are contradictory and the resulting file mixes intent; verify a single consistent version is used.
- qr-preserves-behavior (`cmd/otel_middleware.go`): authOutcomeMiddleware reuses the statusRecorder installed upstream via a type assertion on w, but by the time the chain reaches it the ResponseWriter has been wrapped by otelchi.Middleware and httpTelemetryMiddleware's own statusRecorder — however the intervening SimpleCORSMiddleware and otelchi wrap w so the direct type assertion w.(*statusRecorder) will typically fail; more importantly authOutcomeMiddleware reads rec.status AFTER next.ServeHTTP but the JWT validator writes 401/403 to whatever writer it holds, which is the outer statusRecorder — the reason this is fragile is that when the assertion fails it installs a NEW recorder that shadows nothing, so auth denial statuses are still captured, but the outcome only counts on protected routes; acceptable. The real defect: the httpTelemetryMiddleware is registered BEFORE api.SimpleCORSMiddleware and the protected group, so its statusRecorder is the writer, but authOutcomeMiddleware runs inside the group after CORS which does not re-wrap — the assertion succeeds. Omit.
- qr-metric-naming (`cmd/telemetry.go`): The new counter is named http.server.request.count with unit "1"; per semconv style plain counts are unsuffixed, so the ".count" suffix is redundant/non-conformant and inconsistent with the sibling http.server.active_requests naming.
- general (`cmd/telemetry.go`): go.mod pins go.opentelemetry.io/otel and related modules to v1.36.0 in the file content while the changes hunk and proposedDependencies list v1.35.0, an internal inconsistency in the dependency set.
- qr-preserves-behavior (`cmd/otel_middleware.go`): authOutcomeMiddleware reuses the statusRecorder installed by httpTelemetryMiddleware, but SimpleCORSMiddleware runs between them and there is no guarantee it doesn't wrap the writer; the type assertion w.(*statusRecorder) will typically fail because CORS/other middleware may pass a different writer, and even when it succeeds it reads rec.status which is only set after the full downstream chain returns — this generally works but the assumption is fragile. Not a hard defect.
- qr-metric-naming (`cmd/telemetry.go`): New metric name 'http.server.request' (the outcome counter) collides conceptually with the semconv histogram 'http.server.request.duration' and is an unconventional bare name for a count; semconv uses 'http.server.request.duration' for the histogram and there is no plain 'http.server.request' counter, making the name ambiguous.

</details>
**Outstanding (1):**
- ⚠ qr-metric-naming (`cmd/telemetry.go`:40): The new counter is named "http.server.request.count" with unit "1", but semconv/repo style leaves plain counts unsuffixed; the ".count" suffix encodes the aggregation and conflicts with the convention. Prefer "http.server.requests" or the semconv name without a count suffix.

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `cmd/server.go`
- `go.mod`

## New files
- `cmd/telemetry.go`
- `cmd/otel_middleware.go`

## Dependencies
- `go.opentelemetry.io/otel v1.35.0`
- `go.opentelemetry.io/otel/trace v1.35.0`
- `go.opentelemetry.io/otel/metric v1.35.0`
- `go.opentelemetry.io/otel/sdk v1.35.0`
- `go.opentelemetry.io/otel/sdk/metric v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.35.0`
- `go.opentelemetry.io/contrib/instrumentation/github.com/go-chi/chi/otelchi v0.53.0`
- `google.golang.org/grpc (transitive, required by the OTLP gRPC exporters)`
<!-- houndos:entity go-rest-api end -->
