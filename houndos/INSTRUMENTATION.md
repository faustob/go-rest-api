# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity go-rest-api start -->
## Go-rest-api

Generated by HoundOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 1 generation pass — ⚠ passed with warnings.
**Outstanding (4):**
- ⚠ qr-otel-version-pin (`go.mod`:17): go.opentelemetry.io/otel is pinned at 1.36.0, but this job targets the OTel 1.46 line (default-latest). Different PRs against one repo pinning different OTel lines makes the dependency move for no stated reason. — _Pin the OTel dependencies to the 1.46 line, the version this job resolved from the knowledge base._
- ⚠ qr-metric-naming (`pkg/telemetry/otel.go`:45): The new counter is named "http.server.requests", which is not in managedMetricNames; the platform prescribes "http.server.request.outcomes" for the request outcome/tenant counter. This name drift means the SLI-to-signal exact-name join will miss the metric. — _Rename the counter to "http.server.request.outcomes" (or split into the prescribed managed names) to match the managedMetricNames list._
- ⚠ general (`go.mod`:18): The go.mod content block pins the otel modules at v1.36.0 while the changes hunk and proposedDependencies specify v1.31.0; the file's require block and the diff hunk disagree, which is an inconsistent dependency state. — _Make the go.mod content and the change hunk agree on a single otel version consistent with the SDK/semconv usage._
- ⚠ go-rest-api-http-auth-failure-rate (`pkg/telemetry/middleware.go`:140): AuthOutcomeMiddleware reads rec.status after next.ServeHTTP, but it is mounted before jwtValidator.Middleware. Because the auth middleware wraps the validator, it does observe the validator's rejection status, so this is wired — but the counter counts only requests on protected routes, and success on protected routes is recorded as an auth 'attempt' even when no auth is checked, which may distort the auth failure rate denominator. — _Restrict auth.attempts recording to requests that actually underwent authentication (e.g. only record when the validator ran), so the failure-rate denominator reflects real auth attempts._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute

## Modified files
- `cmd/server.go`
- `go.mod`

## New files
- `pkg/telemetry/otel.go`
- `pkg/telemetry/middleware.go`

## Dependencies
- `go.opentelemetry.io/otel v1.31.0`
- `go.opentelemetry.io/otel/metric v1.31.0`
- `go.opentelemetry.io/otel/trace v1.31.0`
- `go.opentelemetry.io/otel/sdk v1.31.0`
- `go.opentelemetry.io/otel/sdk/metric v1.31.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.31.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.31.0`
<!-- houndos:entity go-rest-api end -->
