# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity go-rest-api start -->
## Go-rest-api

Generated by HoundOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 4 generation passes — ⚠ passed with warnings.
<details><summary>9 issues found and fixed during self-review</summary>

- qr-endpoint-env-driven (`cmd/server.go`): Hardcoded OTLP endpoint "localhost:4317" — wrong in every real deployment.
- qr-preserves-behavior (`cmd/routes.go`): deleteThing calls resp.WriteHeader(http.StatusNoContent) then api.ReturnText(resp, "Thing deleted"), but this ordering is pre-existing and not introduced by the diff. Omitted.
- qr-not-a-noop (`cmd/server.go`): authTelemetryMiddleware and jwtValidator.Middleware are registered inside a chi Group whose only routes are the protected create/update/delete routes; the auth counter therefore only observes protected traffic, which is fine, but the middleware order comment claims to observe rejected requests — this is consistent so no defect.
- qr-endpoint-env-driven (`cmd/server.go`): Hardcoded OTLP endpoint "localhost:4317" — wrong in every real deployment.
- qr-preserves-behavior (`cmd/server.go`): authTelemetryMiddleware wraps the ResponseWriter in a statusCapturingWriter and passes it to next.ServeHTTP without forwarding the request context, but more importantly it is registered inside the protected group AFTER httpTelemetryMiddleware which already wrapped the writer; the double-wrapping is benign, but authTelemetryMiddleware calls next.ServeHTTP(sw, req) dropping the context established upstream — this severs span propagation for the protected routes.
- general (`go.mod`): go.mod pins go.opentelemetry.io/otel and related modules to v1.36.0 in the require block while proposedDependencies and the diff hunk specify v1.28.0, an internal inconsistency the compiler/module resolver will surface.
- qr-metric-naming (`cmd/server.go`): New metric names mix namespacing styles: HTTP metrics use the semconv http.server.* prefix while flow metrics use a bare flow.* prefix with no app/domain root and generic nouns like flow.entries and flow.outcomes that do not state what is measured in the repo's convention.
- qr-endpoint-env-driven (`cmd/server.go`): Hardcoded OTLP endpoint "localhost:4317" — wrong in every real deployment.
- qr-preserves-behavior (`cmd/routes.go`): In deleteThing the diff calls recordValidationOutcome(req, "thing.id", "passed") on the success path, but this handler writes WriteHeader(204) then ReturnText — the pre-existing 204 body write is unchanged. The added telemetry call itself is fine; however the auth telemetry middleware wrapping introduces a behavior issue elsewhere, noted separately.

</details>
**Outstanding (2):**
- ⚠ qr-endpoint-env-driven (`cmd/server.go`:153): Hardcoded OTLP endpoint "http://localhost:4318" — wrong in every real deployment. — _Read the endpoint from OTEL_EXPORTER_OTLP_ENDPOINT / config instead of a literal._
- ⚠ qr-metric-naming (`cmd/server.go`:72): The new counter names use an inconsistent prefix style: 'auth.attempts' and 'flow.*' domain-prefixed names sit alongside 'http.server.request.duration' semconv names, but 'auth.attempts' is not a semconv name and lacks a consistent service/domain prefix pattern with the flow metrics. Consider aligning to one convention (e.g. semconv http.server.* plus a single domain prefix for custom metrics). — _Establish one consistent prefix for the non-semconv custom metrics (e.g. all under 'flow.*' / a service domain), rather than mixing 'auth.attempts' with 'flow.request.count'._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `cmd/server.go`
- `cmd/routes.go`
- `go.mod`

## Dependencies
- `go.opentelemetry.io/otel v1.28.0`
- `go.opentelemetry.io/otel/metric v1.28.0`
- `go.opentelemetry.io/otel/sdk v1.28.0`
- `go.opentelemetry.io/otel/sdk/metric v1.28.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.28.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.28.0`
<!-- houndos:entity go-rest-api end -->
