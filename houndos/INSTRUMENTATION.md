# Instrumentation

Generated by HoundOS — one section per instrumented entity; each
instrumentation PR updates only its own entity's section.

<!-- houndos:entity go-rest-api start -->
## Go-rest-api

Generated by HoundOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 1 generation pass — ⚠ passed with warnings.
**Outstanding (4):**
- ⚠ qr-endpoint-env-driven (`cmd/telemetry.go`:159): Hardcoded OTLP endpoint "localhost:4317" — wrong in every real deployment. — _Read the endpoint from OTEL_EXPORTER_OTLP_ENDPOINT / config instead of a literal._
- ⚠ general (`go.mod`:1): go.mod's require block pins the OTel modules to v1.36.0 in the top require section while the diff's changes hunk adds a duplicate require block pinning the same modules to v1.31.0, and proposedDependencies list v1.31.0. This inconsistent/duplicated version pin will fail to build cleanly until reconciled. — _Reconcile the OTel module versions to a single consistent set of pins (remove the duplicate require entries)._
- ⚠ qr-metric-naming (`cmd/telemetry.go`:68): The counter name "http.server.request.outcomes" pluralizes the measured noun as a suffix, which diverges from semconv-style unsuffixed count naming and from the sibling "http.server.request.count"; two different count-naming conventions appear in the same diff. — _Use a single count convention, e.g. name it http.server.request.count or fold outcome into an attribute on one request counter rather than a pluralized name._
- ⚠ sli-not-implemented (`cmd/telemetry.go`:225): The flow.* metrics (flowEntries/flowOutcomes/flowDuration) treat every single HTTP request as one complete business flow entry-and-terminal in the same middleware, so flow-primary-flow-e2e-flow-freshness (P95 entry-to-terminal, up to 2 minutes, distinct from per-request handler duration) is not actually measured — flowDuration merely re-records the same per-request handler duration as the HTTP latency histogram. — _Instrument the actual multi-step flow entry and terminal points, or record freshness against a genuine entry-to-terminal span rather than aliasing per-request handler duration._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `cmd/server.go`
- `cmd/routes.go`
- `go.mod`

## New files
- `cmd/telemetry.go`

## Dependencies
- `go.opentelemetry.io/otel v1.31.0`
- `go.opentelemetry.io/otel/trace v1.31.0`
- `go.opentelemetry.io/otel/metric v1.31.0`
- `go.opentelemetry.io/otel/sdk v1.31.0`
- `go.opentelemetry.io/otel/sdk/metric v1.31.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.31.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.31.0`
- `go.opentelemetry.io/contrib/instrumentation/github.com/go-chi/chi/v5/otelchi v0.56.0`
<!-- houndos:entity go-rest-api end -->
