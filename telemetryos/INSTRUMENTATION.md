# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (8):**
- general (`cmd/server.go`): The file uses http.Handler in the otelhttp middleware (`func(next http.Handler) http.Handler`) but the import block never imports "net/http", so the package will not compile.
- general (`cmd/server.go`): The final file content has a stray extra closing brace after main() (the body ends with `api.StartServer(...)\n}\n}`), producing an unbalanced brace and a compile error.
- qr-not-a-noop (`cmd/telemetry.go`): initSLIMetrics() is never called from main() (only initOTel is invoked), so `sli` stays nil; the sliMiddleware short-circuits via `if sli == nil` and none of the flow/auth/duration instruments are ever created or recorded, emitting no telemetry.
- general (`cmd/telemetry.go`): Inside the RegisterCallback closure the code declares a local variable named `import_runtime_maxprocs` via `:=` and assigns it from runtimeGOMAXPROCS(); this is fine, but `runtimeGOMAXPROCS` in turn assigns `import_runtime := runtimeMaxProcs()` — both compile only if runtimeMaxProcs exists, which it does. However the identifier `import_runtime_maxprocs`/`import_runtime` are just oddly named locals and compile; the real defect is that sliMiddleware is never wired into the router, so its instruments never record.
- qr-preserves-behavior-static (`cmd/server.go`): Edit removed a control-flow statement: "return" appears 1× in the original snippet but 0× after the edit — behavior changed.
- general (`go.mod`): The diff adds direct requires for otel packages but does not add go.opentelemetry.io/otel/metric, go.opentelemetry.io/otel/trace, go.opentelemetry.io/otel/attribute, or the semconv/v1.26.0 module version entries, and there is no corresponding go.sum. The build will fail on missing module requirements/checksums for the newly imported packages.
- qr-preserves-behavior (`cmd/telemetry.go`): The Recoverer middleware is registered AFTER sliMiddleware in the chain, so a panic in a downstream handler propagates up through sliMiddleware and is recovered above it; the deferred inFlightRequests.Add(-1) still runs but flowOutcomes/flowDuration are never recorded, and more importantly the otelhttp and sli middleware wrap the handler such that a panic is now caught by Recoverer which sits below them — this is pre-existing ordering. Not flagged.
- qr-preserves-behavior (`cmd/telemetry.go`): sliMiddleware wraps the ResponseWriter with statusWriter unconditionally for every request (including /metrics and /health), adding a wrapper layer to the hot path; because it is registered before RealIP and Recoverer it changes the ResponseWriter type seen by all downstream handlers, which can defeat chi's own WrapResponseWriter used by the request logger.
**Outstanding (3):**
- ⛔ qr-not-a-noop (`cmd/telemetry.go`:33): initSLIMetrics() calls otel.Meter() and creates all instruments only if initOTel() succeeded, but if initOTel returns an error the global MeterProvider is never set and all instruments resolve to no-ops; however this is handled gracefully. The real problem: the sliMetrics uses the OTLP metric exporter but telemetryCollection is "none", so otlpmetricgrpc/otlptracegrpc exporters will attempt to connect to a nonexistent collector — but connection is lazy so this is acceptable. Retained concern: none.
- ⛔ qr-preserves-behavior (`cmd/telemetry.go`:180): The statusWriter wrapper does not implement http.Hijacker, http.Pusher, or io.ReaderFrom, so wrapping every handler strips these interfaces from the underlying ResponseWriter; any downstream handler relying on Hijack (e.g. websockets/SSE) will fail a type assertion that previously succeeded, changing existing behavior. — _Preserve optional interfaces by delegating Hijack/Push/ReadFrom, or use a well-tested wrapper such as chi's middleware.WrapResponseWriter._
- ⛔ edit-not-applied (`cmd/server.go`): An intended edit to cmd/server.go could not be applied (its target text wasn't found verbatim, wasn't unique, or overlapped another edit), so the file was left unchanged to avoid a half-applied result: Configure otelhttp middleware with message events for richer span data — _Re-express this as one or more SMALL hunks whose oldString is copied verbatim from the CURRENT source and is unique; don't restate a large block. Keep declarations and their uses in edits that apply together._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`

## New files
- `cmd/telemetry.go`
- `cmd/runtime_helpers.go`

## Dependencies
- `go.opentelemetry.io/otel v1.35.0`
- `go.opentelemetry.io/otel/sdk v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.35.0`
- `go.opentelemetry.io/otel/sdk/metric v1.35.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.60.0`

## Edits that need manual application
These instrumentation changes couldn't be applied automatically (the target code had moved or
wasn't unique). Apply them by hand:

### `cmd/server.go` — Configure otelhttp middleware with message events for richer span data
```
	// OpenTelemetry HTTP middleware — emits http.server.request.duration histogram
	// and active-request tracking per OTel semantic conventions
	router.Use(func(next http.Handler) http.Handler {
		return otelhttp.NewMiddleware(serviceName,
			otelhttp.WithMessageEvents(otelhttp.ReadEvents, otelhttp.WriteEvents),
		)(next)
	})
```

