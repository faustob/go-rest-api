# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (6):**
- qr-duplicate-file-path (`cmd/server.go`): "cmd/server.go" appears 2× in the changeset (edited file + edited file) — two conflicting full contents for one path. Only the last-written wins, and it may drop declarations/imports the other version added, so the file won't compile.
- qr-duplicate-file-path (`cmd/routes.go`): "cmd/routes.go" appears 3× in the changeset (edited file + edited file + edited file) — two conflicting full contents for one path. Only the last-written wins, and it may drop declarations/imports the other version added, so the file won't compile.
- qr-not-a-noop (`cmd/otel.go`): The package-level `tracer` is initialized via `otel.Tracer(...)` at package-load, and setupOTel builds and registers ONLY a TracerProvider — it never creates a MeterProvider nor calls otel.SetMeterProvider. Consequently `meter = otel.Meter(...)` resolves to the global no-op meter and every custom instrument (requestOutcomeCounter, flowOutcomeCounter, histograms, etc.) records to nothing.
- qr-not-a-noop (`cmd/telemetry_middleware.go`): TelemetryOutcomeMiddleware (which records flow entry/outcome/duration/freshness counters and the request-outcome counter) is defined but never added to the router via router.Use in cmd/server.go, so none of these custom instruments are ever invoked at runtime.
- general (`cmd/telemetry_middleware.go`): statusCapturingWriter embeds http.ResponseWriter but only reimplements Flush; it does not implement http.Hijacker or io.ReaderFrom, so wrapping the writer silently drops those optional interfaces the underlying writer may have provided, breaking websocket/hijack behavior for any handler that needs it.
- general (`cmd/otel.go`): attributeErrorType uses semconv.ErrorTypeKey and semconv.ServiceName from go.opentelemetry.io/otel/semconv/v1.26.0; the auth counter and referenced pkg/auth/jwt.go recording described in comments do not exist in the diff, and authAttemptsCounter is created but never used.
**Outstanding (3):**
- ⚠ qr-preserves-behavior (`cmd/otel.go`:18): Two package-level instruments (authAttemptsCounter, flowValidationCounter is initialized but authAttemptsCounter is declared and never assigned in initInstruments nor used) — authAttemptsCounter is a declared but unused package variable, which the compiler gate will flag; also flowValidationCounter is created but never recorded anywhere so it emits no data. — _Remove the unused authAttemptsCounter declaration and either wire flowValidationCounter to real recording sites or drop it._
- ⚠ qr-preserves-behavior (`cmd/telemetry_middleware.go`:90): The statusCapturingWriter wrapper unconditionally wraps every response and only forwards Flusher/Hijacker/ReaderFrom, but does not implement http.Pusher or io.StringWriter; more importantly the middleware runs before the otelchi/RealIP/Recoverer chain established in server.go, so wrapping here changes the ResponseWriter type seen by downstream middleware. — _Confirm no downstream middleware type-asserts for interfaces not forwarded; consider ordering the outcome middleware after otelchi and using a response recorder that forwards all standard interfaces._
- ⚠ qr-not-a-noop (`cmd/otel.go`:54): tracer is assigned at package init via otel.Tracer(...) before setupOTel registers the provider; while it is re-assigned inside setupOTel, routes.go and telemetry_middleware.go use trace.SpanFromContext (context-based) rather than the package tracer, and the package-level tracer variable is never actually used to start spans, making it dead. — _Remove the unused package-level tracer variable or use it to create real spans._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`
- `cmd/routes.go`

## New files
- `cmd/otel.go`
- `cmd/telemetry_middleware.go`

## Dependencies
- `go.opentelemetry.io/otel v1.36.0`
- `go.opentelemetry.io/otel/sdk v1.36.0`
- `go.opentelemetry.io/otel/sdk/metric v1.36.0`
- `go.opentelemetry.io/otel/metric v1.36.0`
- `go.opentelemetry.io/otel/trace v1.36.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.36.0`
- `go.opentelemetry.io/contrib/instrumentation/github.com/go-chi/chi/otelchi v0.56.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.36.0`
