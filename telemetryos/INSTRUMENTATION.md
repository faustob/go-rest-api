# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (8):**
- general (`cmd/server.go`): The otelhttp.NewMiddleware span-name-formatter callback references *http.Request but cmd/server.go does not import "net/http", so the file fails to compile.
- general (`cmd/routes.go`): cmd/routes.go imports "context" and "fmt" but neither is used in the file (handlers use req.Context(), not context directly, and fmt is never referenced), causing a compile error for unused imports.
- general (`cmd/routes.go`): The package-level vars authAttempts and authAttemptsCounter (cmd/auth_telemetry.go) are declared but never used; unused package-level vars in Go are allowed to compile, but authAttempts in routes.go is a declared-and-unused package var only via blank-less var which is fine — however the duplicate instrument name "auth.attempts" is registered by two meters. This does not compile-fail; retained instead: authAttempts in routes.go is genuinely never referenced.
- qr-preserves-behavior (`cmd/saturation.go`): registerSaturationGauges registers an observable gauge with the same instrument name "http.server.active_requests" as the Int64UpDownCounter of the same name in routes.go; registering two instruments with identical name but conflicting types/kinds produces an OTel duplicate-instrument conflict warning and one instrument is dropped, so the metric may not export correctly.
- qr-not-a-noop (`cmd/saturation.go`): registerSaturationGauges is defined but is never called from main(), so the saturation gauges and their callback are never registered and emit no telemetry.
- general (`cmd/routes.go`): The import block includes "context" and "fmt" (added in an earlier hunk and never removed by the final import hunk), but the final routes.go file uses neither package. Go treats unused imports as compile errors.
- qr-preserves-behavior (`cmd/routes.go`): In deleteThing the instrumentation preserves the pre-existing WriteHeader(204)+ReturnText body write, but that is out of scope; the real added issue is none. Omit.
- general (`cmd/auth_telemetry.go`): authTelemetryMiddleware and authStatusRecorder are defined but never wired into the router in server.go, so the auth.attempts counter is never emitted at runtime.
**Outstanding (2):**
- ⛔ general (`cmd/routes.go`:10): The final import block for routes.go imports "context" and "fmt" but neither is used anywhere in the file, which is a compile error in Go (imported and not used). — _Remove the "context" and "fmt" imports from cmd/routes.go; the final content should not include them (as the later hunk intends)._
- ⛔ qr-preserves-behavior (`cmd/auth_telemetry.go`:47): authTelemetryMiddleware wraps the response in authStatusRecorder which only overrides WriteHeader, discarding the underlying ResponseWriter's implementation of http.Flusher/Hijacker/Pusher; this changes existing behavior for handlers/middleware that rely on those optional interfaces (e.g. streaming/flushing). — _Do not wrap the ResponseWriter, or implement the optional interfaces (Flush/Hijack/Push) with type-assertion passthroughs on authStatusRecorder._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`
- `cmd/routes.go`

## New files
- `cmd/auth_telemetry.go`
- `cmd/saturation.go`

## Dependencies
- `go.opentelemetry.io/otel v1.35.0`
- `go.opentelemetry.io/otel/sdk v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.35.0`
- `go.opentelemetry.io/otel/sdk/metric v1.35.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.60.0`
