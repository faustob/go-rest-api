# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (6):**
- qr-not-a-noop (`cmd/saturation_metrics.go`): registerSaturationMetrics() is defined but never called from main() or any init(), so the observable gauge http.server.worker_pool.size and its callback are never registered and emit nothing.
- qr-not-a-noop (`cmd/auth_metrics.go`): AuthMetricsMiddleware is defined but never wired into the router (the protected group only uses jwtValidator.Middleware), so auth.attempts is never recorded and the auth-failure-rate SLI emits no data.
- general (`cmd/routes.go`): routes.go references otelTrace.WithAttributes but does not import the trace package; the alias declared in otel_trace_alias.go is package-scoped to a different file and does not make otelTrace a usable identifier in routes.go.
- qr-preserves-behavior (`cmd/auth_metrics.go`): init() may set authAttemptsCounter to nil if Int64Counter returns an error (the error is swallowed), and AuthMetricsMiddleware then calls authAttemptsCounter.Add on a possibly-nil counter; also the SDK MeterProvider is only registered later in main() via initOTel, so init() binds against the default no-op provider unless proxy delegation applies.
- qr-preserves-behavior (`cmd/routes.go`): In deleteThing the instrumentation adds flowOutcomesCounter.Add and flowDurationHistogram.Record AFTER resp.WriteHeader(http.StatusNoContent) and api.ReturnText. The pre-existing WriteHeader(204)-then-write quirk is preserved, so this is not a new behavior change, but the added metric calls run correctly after the write — no defect here.
- qr-preserves-behavior (`cmd/auth_metrics.go`): AuthMetricsMiddleware is registered AFTER jwtValidator.Middleware in the protected group, so it wraps handlers that run only after JWT validation passes; a request rejected by the JWT middleware (401/403) never reaches AuthMetricsMiddleware, so denied outcomes are never recorded and the auth-failure-rate SLI it intends to power will be blind to actual denials.
**Outstanding (1):**
- ⛔ qr-preserves-behavior (`cmd/routes.go`): In deleteThing the added flowOutcomesCounter.Add / flowDurationHistogram.Record and AddEvent run after resp.WriteHeader(http.StatusNoContent) and api.ReturnText(resp, "Thing deleted"); those metric calls themselves don't alter the response, but this is unchanged app behavior — the actual instrumentation-introduced concern is none here, so this is not flagged.

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`
- `cmd/routes.go`

## New files
- `cmd/otel_trace_alias.go`
- `cmd/auth_metrics.go`
- `cmd/saturation_metrics.go`

## Dependencies
- `go.opentelemetry.io/otel v1.35.0`
- `go.opentelemetry.io/otel/sdk v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.35.0`
- `go.opentelemetry.io/otel/sdk/metric v1.35.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.60.0`
