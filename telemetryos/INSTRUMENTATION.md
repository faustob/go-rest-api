# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (7):**
- qr-not-a-noop (`cmd/main.go`): Under the app-sdk wiring model this diff must register the SDK, but initOTel() and initMetrics() are never called from main(). The provided cmd/main.go is an empty stub with no main() invoking the bootstrap, so the global providers are never registered and all instruments resolve to no-ops.
- qr-preserves-behavior (`cmd/main.go`): This file declares 'package main' with no imports or code but its header comment claims it is 'otel_init.go'. If the existing project already has a cmd/main.go, adding another cmd/main.go marked isNew=true will collide with the existing file (duplicate path) or, if it is a genuinely new empty file, the referenced initOTel/initMetrics/telemetryMiddleware are never called, breaking the intended wiring.
- general (`cmd/middleware.go`): flowValidationOutcomes is recorded twice per request (once as outcome="attempted" at entry and once as passed/failed at exit), so the counter double-counts every request relative to a normal outcome-classified counter, distorting the validation failure-rate SLI.
- qr-not-a-noop (`cmd/main.go`): This diff defines setupOTel()/wrapWithOTel() but never calls them, and initOTel()/initMetrics() are only invoked from setupOTel(). Under app-sdk, if the existing main() is not modified to call setupOTel(), the SDK is never registered and globalMeter/all instruments resolve to no-ops; nothing in this diff wires the bootstrap into the actual entry point.
- general (`cmd/main.go`): The comment claims main() lives in the repository's existing entry-point file, but this new file is named cmd/main.go in package main. If the repo already has a cmd/main.go, this creates a duplicate file/path collision; if the intent is a separate helper file, it is misnamed and misleading.
- general (`cmd/otel.go`): resource.New with resource.WithFromEnv/WithProcess/WithOS combined with the default resource can return a *resource.ErrSchemaURLConflict error when the SDK's built-in schema URL differs from semconv/v1.26.0; treating this partial-merge error as fatal (returned to setupOTel which log.Fatalf) can abort startup even though a usable resource is produced.
- qr-preserves-behavior (`cmd/middleware.go`): The statusRecorder wrapper only implements WriteHeader and embeds http.ResponseWriter, so it does not preserve optional interfaces (http.Flusher, http.Hijacker, io.ReaderFrom) of the underlying writer; wrapping every request can break streaming/SSE, WebSocket upgrades, or efficient file serving that existing handlers rely on.
**Outstanding (2):**
- ⛔ general (`cmd/main.go`:40): main.go defines its own ThingAPI type and handler methods (getThings, getThingByID, createThing, deleteThing) as if they exist, but only ThingAPI struct is declared here with no method definitions; these handler methods are referenced but never defined in the diff, causing a compile error unless they exist elsewhere in package main with matching signatures. — _Ensure the ThingAPI handler methods (getThings, getThingByID, createThing, deleteThing) exist in the package, or reference the pre-existing router/handlers rather than redeclaring ThingAPI._
- ⚠ qr-preserves-behavior (`cmd/main.go`:40): main.go redeclares main() and the router setup from scratch as a new file, which will conflict with the repository's existing main package entry point (duplicate main function / duplicate route wiring), breaking compilation and replacing existing behavior. — _Add OTel bootstrap and middleware wiring into the existing main.go rather than introducing a second main() and router in package main._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`

## New files
- `cmd/otel.go`
- `cmd/metrics.go`
- `cmd/middleware.go`
- `cmd/main.go`

## Dependencies
- `go.opentelemetry.io/otel v1.33.0`
- `go.opentelemetry.io/otel/sdk v1.33.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.33.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.33.0`
- `go.opentelemetry.io/otel/metric v1.33.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.58.0`
