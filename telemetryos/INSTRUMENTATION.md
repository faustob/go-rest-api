# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (7):**
- qr-not-a-noop (`pkg/telemetry/telemetry.go`): initInstruments() runs first at package-load init() against the no-op meter, binding requestDuration/requestOutcome/flowOutcomes/flowDuration/authAttempts/validationOutcome to no-op instruments. The observable-gauge RegisterCallback is registered on the no-op meter and is never re-registered by SetupOTelSDK's second initInstruments call, so the active_requests and worker_pool.size gauges never emit against the real SDK.
- qr-preserves-behavior (`pkg/telemetry/telemetry.go`): AuthOutcomeMiddleware wraps the response in a statusRecorder that no longer exposes the wrapped ResponseWriter's other optional interfaces uniformly and, more importantly, changes the JWT middleware's control flow: the original protectedRouter.Use(jwtValidator.Middleware) aborted the chain on auth failure, but here the recorder is always constructed and the auth outcome logic runs after ServeHTTP — however the real behavior change is that the recorder's Write defaults status to 200, so a JWT middleware that writes 401 via Write without WriteHeader is still captured correctly, but downstream handlers that type-assert the ResponseWriter to interfaces the recorder doesn't implement (e.g. http.CloseNotifier or http.Pusher) will lose them.
- qr-preserves-behavior (`cmd/server.go`): The final content uses log.Fatalf on OTel setup failure, which calls os.Exit and terminates the whole server if the OTLP exporter cannot be created — turning a telemetry setup failure into a hard startup crash where the original app had no such dependency.
- qr-preserves-behavior (`pkg/telemetry/telemetry.go`): The statusRecorder wraps the ResponseWriter unconditionally in HTTPMetricsMiddleware and AuthOutcomeMiddleware, but it only implements Flush/Hijack/ReadFrom via type assertions on the wrapped writer. Because statusRecorder always satisfies http.Flusher/Hijacker/ReaderFrom at the type level, downstream handlers that type-assert for these interfaces now always succeed even when the original writer did not support them, changing behavior (e.g. Hijack returns an error instead of the assertion failing).
- qr-preserves-behavior (`pkg/telemetry/telemetry.go`): SetupOTelSDK re-creates all instruments and re-registers the same-named observable-gauge callback via initInstruments() a second time (once in init(), once after SDK setup) against the new MeterProvider; the first callback is registered against the no-op provider so this is harmless, but calling initInstruments twice against the real provider is not possible here — the real risk is the duplicated flowOutcomes.Add(ctx,0) no-op touch emitting a spurious zero-value data point on every request.
- qr-preserves-behavior (`cmd/server.go`): SetupOTelSDK dials an OTLP gRPC exporter (otlptracegrpc.New/otlpmetricgrpc.New) and any failure now triggers log.Fatalf, so if no collector/OTLP endpoint is reachable the server exits at startup. This turns instrumentation into a hard startup dependency where none existed before.
- general (`pkg/telemetry/telemetry.go`): The declared package variable sdkConfigured is never assigned or read, and the ReadFrom method's non-standard signature (using an inline interface rather than io.Reader) means it does not implement io.ReaderFrom, so it will not be picked up by callers expecting that interface.
**Outstanding (3):**
- ⛔ qr-not-a-noop (`pkg/telemetry/telemetry.go`:218): HTTPMetricsMiddleware and AuthOutcomeMiddleware are registered via router.Use() in main() and executed on every request, but tracer, meter, and all instrument globals are only assigned inside SetupOTelSDK/initInstruments. If SetupOTelSDK returns an error (e.g. the OTLP gRPC exporter cannot be created — telemetryCollection is 'none', so no collector exists), main only logs and continues, leaving tracer nil and every instrument nil, causing a nil-pointer panic on tracer.Start / requestDuration.Record for every request. — _Initialize the package tracer/meter and instruments unconditionally at package init (bound to the global no-op provider), or make main() fatal-exit when SetupOTelSDK fails, so the middlewares never dereference nil instruments._
- ⛔ qr-preserves-behavior (`pkg/telemetry/telemetry.go`:225): activeRequestsCount is incremented/decremented from HTTPMetricsMiddleware on every concurrent request goroutine with a plain int64 and no synchronization, while the observable-gauge callback reads it concurrently; this is a data race that will fail under -race and can corrupt the counter. — _Use sync/atomic (atomic.AddInt64/atomic.LoadInt64) for activeRequestsCount, or an int64 accessed under a mutex._
- ⚠ qr-preserves-behavior (`pkg/telemetry/telemetry.go`:300): AuthOutcomeMiddleware wraps the JWT middleware but always calls next via wrapped.ServeHTTP and then records metrics based on status code; if the underlying jwtMiddleware short-circuits by writing a 401/403 the recorder captures it correctly, but wrapping the ResponseWriter in a statusRecorder drops the Flusher/Hijacker interfaces despite the comment claiming they are preserved, which can break streaming/websocket upgrades on protected routes. — _Implement http.Flusher/http.Hijacker pass-through on statusRecorder or use an existing wrapped-writer helper that preserves these interfaces._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `cmd/server.go`
- `go.mod`

## New files
- `pkg/telemetry/telemetry.go`

## Dependencies
- `go.opentelemetry.io/otel v1.36.0`
- `go.opentelemetry.io/otel/sdk v1.36.0`
- `go.opentelemetry.io/otel/metric v1.36.0`
- `go.opentelemetry.io/otel/trace v1.36.0`
- `go.opentelemetry.io/otel/sdk/metric v1.36.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.36.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.36.0`
