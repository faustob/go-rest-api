# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (7):**
- qr-not-a-noop (`cmd/server.go`): protectedRouter.Use(auth.MetricsMiddleware) references an undefined symbol; there is no auth.MetricsMiddleware defined anywhere in the diff, and the intended metrics instrument accessor (otelinit.Meter) is never used, so the promised custom auth metrics emit nothing. Note the two subsequent hunks add and then remove this line, so the final content omits it, meaning no custom metric middleware is wired at all.
- qr-not-a-noop (`pkg/otelinit/metrics.go`): The Meter() accessor is never called anywhere in the diff and Tracer() returns a nil empty-interface, so no custom instruments are ever created or recorded; the shared meter accessor produces no telemetry.
- general (`pkg/otelinit/otelinit.go`): otlptracegrpc.New/otlpmetricgrpc.New are called with WithEndpoint(endpoint) where endpoint may be empty if OTEL_EXPORTER_OTLP_ENDPOINT is unset, which passes an empty host and will fail exporter dialing/creation rather than defaulting to localhost:4317.
- general (`cmd/server.go`): The otelhttp middleware uses http.Handler/http.Handler but the net/http package is not imported in cmd/server.go, so the middleware closure references an undeclared package.
- qr-not-a-noop (`cmd/server.go`): otelinit.Init returns an error when OTEL_EXPORTER_OTLP_ENDPOINT is unset, and main only logs it and continues with a nil shutdown; in that case no SDK provider is ever registered and all otelhttp/custom telemetry resolves to the no-op provider, emitting nothing.
- qr-preserves-behavior (`cmd/server.go`): The diff's changes reference protectedRouter.Use(auth.MetricsMiddleware) being removed, but the final content no longer adds any auth metrics middleware, and there is no auth.MetricsMiddleware symbol defined in this diff; the inconsistent hunks indicate leftover references that may not resolve.
- general (`pkg/otelinit/metrics.go`): The Tracer() function declares a return type of an empty interface and always returns nil, making it a useless accessor that cannot provide a real tracer to callers.
**Outstanding (4):**
- ⚠ qr-endpoint-env-driven (`pkg/otelinit/otelinit.go`:38): Hardcoded OTLP endpoint "localhost:4317" — wrong in every real deployment. — _Read the endpoint from OTEL_EXPORTER_OTLP_ENDPOINT / config instead of a literal._
- ⚠ general (`cmd/server.go`:56): The file uses http.Handler and otelhttp.NewHandler in the router.Use middleware but the "net/http" package is never imported in cmd/server.go, so http.Handler is undefined. — _Add "net/http" to the import block in cmd/server.go._
- ⚠ general (`cmd/server.go`:76): router.Use(auth.MetricsMiddleware) references a symbol that does not appear to exist in the auth package (only NewJWTValidator is used elsewhere); if MetricsMiddleware is undefined this will not compile. — _Confirm auth.MetricsMiddleware exists, or remove the added middleware line._
- ⛔ qr-preserves-behavior (`cmd/server.go`:47): otelinit.Init calls otlptracegrpc.New/otlpmetricgrpc.New and builds providers with a batcher/periodic reader targeting localhost:4317, but telemetryCollection is "none" so no collector exists; this adds startup connection attempts and background export goroutines with no receiver. The Init failure is only logged (non-fatal), so behavior is preserved, but the periodic exporter will repeatedly attempt connections. This is acceptable given gRPC exporters are lazy, so not blocking on connect. — _None required for connect; retained only if collector is expected._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `cmd/server.go`
- `go.mod`
- `cmd/routes.go`

## New files
- `pkg/otelinit/otelinit.go`
- `pkg/otelinit/metrics.go`

## Dependencies
- `go.opentelemetry.io/otel v1.36.0`
- `go.opentelemetry.io/otel/sdk v1.36.0`
- `go.opentelemetry.io/otel/metric v1.36.0`
- `go.opentelemetry.io/otel/sdk/metric v1.36.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.36.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.36.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.61.0`
