# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (6):**
- general (`cmd/server.go`): There is a stray extra closing brace `}` after the closing brace of main(), producing a syntax error that prevents compilation.
- general (`cmd/server.go`): The file uses `http.Handler` in the otelhttp middleware wrapper but does not import `net/http`; the package will fail to compile.
- general (`cmd/otel_trace_alias.go`): Import aliases are file-scoped in Go, not package-scoped; the `otelTrace` alias defined in otel_trace_alias.go is not visible in routes.go, so routes.go's references to `otelTrace.WithAttributes` are undefined and will not compile.
- general (`cmd/server.go`): authInstrumentedMiddleware is defined in auth_middleware.go but never used; the protected router still wires jwtValidator.Middleware directly, so auth.attempts is never recorded and the unused function/imports do not break compile only because it's a package-level func — but the instrumentation intent (auth SLI) is not wired in.
- qr-not-a-noop (`cmd/saturation_observer.go`): registerSaturationObserver() is never called from main(), so the worker_pool.size observable gauge and its callback are never registered and the metric is never emitted.
- qr-preserves-behavior (`cmd/routes.go`): In deleteThing the added instrumentation calls resp.WriteHeader(http.StatusNoContent) and then api.ReturnText(resp, ...), which writes a body and attempts a second header write on a 204 No Content response; this changes existing response behavior versus the original handler.
**Outstanding (1):**
- ⚠ qr-preserves-behavior (`cmd/auth_middleware.go`:21): The statusRecorder only overrides WriteHeader, but the underlying JWT/CORS middleware or handlers may call Write() without an explicit WriteHeader (implicit 200) or the JWT validator may write an error body; if the validator sets status via WriteHeader the recorder captures it, but if any downstream code needs http.Flusher/http.Hijacker/http.Pusher interfaces they are lost, potentially breaking streaming or websocket-style responses that the original chain supported. — _Implement interface pass-through (Flusher/Hijacker) on statusRecorder or use an existing response-recorder that preserves optional interfaces, e.g. chi's middleware.WrapResponseWriter._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`
- `cmd/routes.go`

## New files
- `cmd/otel_trace_alias.go`
- `cmd/auth_middleware.go`
- `cmd/saturation_observer.go`

## Dependencies
- `go.opentelemetry.io/otel v1.33.0`
- `go.opentelemetry.io/otel/sdk v1.33.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.33.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.33.0`
- `go.opentelemetry.io/otel/sdk/metric v1.33.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.58.0`
