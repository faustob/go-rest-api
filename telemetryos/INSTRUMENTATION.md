# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (9):**
- qr-duplicate-file-path (`cmd/telemetry.go`): "cmd/telemetry.go" appears 2× in the changeset (new file + new file) — two conflicting full contents for one path. Only the last-written wins, and it may drop declarations/imports the other version added, so the file won't compile.
- general (`cmd/telemetry.go`): The diff contains two files both at path cmd/telemetry.go in the same package main, and both declare type appMetrics, var globalMetrics, and func initMetrics. This is a duplicate-definition compile error (redeclared in this block).
- general (`cmd/telemetry.go`): The first telemetry.go references undefined functions import_runtime_gomaxprocs_via_closure and import_runtime_observe (the latter is never defined anywhere), and imports "sync/atomic" without using it. This fails to compile.
- general (`cmd/middleware.go`): otelhttp.WithAttributes is passed to metric Counter.Add and Histogram.Record, but those methods require metric.AddOption/metric.RecordOption. otelhttp.WithAttributes returns an otelhttp.Option, so these calls do not compile.
- general (`cmd/middleware.go`): otelhttp.NewHandler's second argument is the operation name string, but newOtelHandler passes serviceName as the operation; more critically the wrapper is defined but the router is never actually wrapped in main() (main only does `_ = otelhttp.NewHandler`), so no server telemetry is emitted and the http server is never started.
- general (`cmd/middleware.go`): middleware.go references otelhttp.NewHandler in newOtelHandler but does not import go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp; the file only imports net/http, time, attribute, metric, and trace. This is a compile error (undefined: otelhttp).
- general (`cmd/main.go`): main.go imports go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp but the import ordering places it before the github.com/go-chi imports without gofmt-standard grouping; more importantly the goimports/gofmt grouping aside, this is valid — but note otelhttp is used. No issue.
- qr-preserves-behavior (`cmd/telemetry.go`): initMetrics assigns globalMetrics only at the very end after all instrument creations succeed, but the observable gauge callback registered via RegisterCallback references poolSizeGauge which is not stored in appMetrics; this is fine, but if initMetrics returns an error partway, globalMetrics stays nil and all instrument calls are silently skipped — acceptable given nil guards.
- general (`cmd/main.go`): The chi router is wired with the same auth_middleware.go authTelemetryMiddleware defined in the diff, but main.go never installs authTelemetryMiddleware anywhere, so auth.attempts and the jwt_auth flow.validation.outcomes metrics are never emitted.
**Outstanding (2):**
- ⛔ general (`cmd/middleware.go`:75): newOtelHandler is defined but never used, and main.go calls otelhttp.NewHandler directly instead. An unused package-level function is not a compile error in Go, but the imports in middleware.go for trace and metric are used elsewhere; however newOtelHandler duplicating main's wiring is dead code — not itself blocking. — _Remove the unused newOtelHandler helper or use it in main.go._
- ⛔ qr-preserves-behavior (`cmd/telemetry.go`:1): cmd/telemetry.go and cmd/otel.go both are package main but the referenced ThingAPI type and its methods (getThings, getThingByID, createThing, deleteThing) are used in main.go yet defined nowhere in this diff; if these files are all newly created in an existing repo whose main is elsewhere, there will be a duplicate main() / undefined ThingAPI compile failure. — _Confirm ThingAPI and its handler methods exist in the package and that no other main() is defined; otherwise the package will not compile._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`

## New files
- `cmd/otel.go`
- `cmd/telemetry.go`
- `cmd/middleware.go`
- `cmd/auth_middleware.go`
- `cmd/main.go`

## Dependencies
- `go.opentelemetry.io/otel v1.33.0`
- `go.opentelemetry.io/otel/sdk v1.33.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.33.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.33.0`
- `go.opentelemetry.io/otel/metric v1.33.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.58.0`
