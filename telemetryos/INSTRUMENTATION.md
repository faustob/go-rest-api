# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (8):**
- qr-duplicate-file-path (`cmd/routes.go`): "cmd/routes.go" appears 3× in the changeset (edited file + edited file + edited file) — two conflicting full contents for one path. Only the last-written wins, and it may drop declarations/imports the other version added, so the file won't compile.
- qr-endpoint-env-driven (`cmd/otel.go`): Hardcoded OTLP endpoint "localhost:4317" — wrong in every real deployment.
- qr-endpoint-env-driven (`cmd/metrics_otel.go`): Hardcoded OTLP endpoint "localhost:4317" — wrong in every real deployment.
- qr-not-a-noop (`cmd/server.go`): main() never calls setupOTelSDK or setupOTelMetrics, so neither the TracerProvider nor MeterProvider is ever built or registered as the global provider. Every custom instrument stays nil and trace.SpanFromContext returns a no-op span, so no telemetry is emitted and Shutdown is never deferred.
- qr-not-a-noop (`cmd/server.go`): The otelchi middleware is imported but never registered on the chi router (no router.Use(otelchi.Middleware(...))), so no server spans are created and the getThingByID/deleteThing SpanFromContext calls always resolve to a no-op span.
- qr-not-a-noop (`cmd/routes.go`): getThingByID/deleteThing reference trace.SpanFromContext and attributeErrorType but neither the go.opentelemetry.io/otel/trace import nor an attributeErrorType helper is defined in the file or diff, so the custom span attribution cannot function.
- qr-not-a-noop (`cmd/metrics_otel.go`): Every declared custom instrument (httpRequestOutcome, authAttempts, flowOutcomes, flowEntries, flowValidationOutcomes, flowDuration, flowFreshness) is created but never recorded anywhere in the diff, so they emit no data points at runtime.
- qr-not-a-noop (`cmd/saturation.go`): saturationTrackingMiddleware is defined but never wired onto the router, so inFlightRequests is never incremented and the active_requests observable gauge always reports zero.
**Outstanding (2):**
- ⚠ qr-not-a-noop (`cmd/otel.go`:33): setupOTelSDK reads OTEL_EXPORTER_OTLP_ENDPOINT into `endpoint` but the value is only used to add otlptracegrpc.WithEndpoint; when unset the exporter falls back to the default localhost:4317, which is fine, but the local `serviceName` shadowing note is cosmetic. No actual defect here. — _None required._
- ⚠ general (`cmd/metrics_otel.go`:1): The custom metric helper functions recordHTTPRequestOutcome, recordAuthAttempt, recordFlowEntry, recordFlowValidationOutcome, and recordFlowOutcome are defined but never called anywhere in the diff, so all of these business/flow/auth/outcome instruments are created but never record any data points — the instrumentation emits nothing at runtime for these metrics. — _Wire the record* helpers into the actual middleware/handlers (e.g. an outcome-tracking middleware in server.go, the JWT validator, and the createThing flow) so the instruments receive measurements._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`
- `cmd/routes.go`

## New files
- `cmd/otel.go`
- `cmd/metrics_otel.go`
- `cmd/saturation.go`

## Dependencies
- `go.opentelemetry.io/otel v1.36.0`
- `go.opentelemetry.io/otel/sdk v1.36.0`
- `go.opentelemetry.io/otel/sdk/metric v1.36.0`
- `go.opentelemetry.io/otel/metric v1.36.0`
- `go.opentelemetry.io/otel/trace v1.36.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.36.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.36.0`
- `go.opentelemetry.io/contrib/instrumentation/github.com/go-chi/chi/otelchi v0.61.0`
- `go.opentelemetry.io/otel/semconv/v1.26.0`
