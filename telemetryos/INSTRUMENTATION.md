# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (6):**
- general (`cmd/otel_trace_alias.go`): routes.go references otelTrace.WithAttributes but the otelTrace import is a package-level import in otel_trace_alias.go, not visible in routes.go. Package-scoped identifiers are not shared across files via a var alias; routes.go has no import named otelTrace, so it fails to compile with 'undefined: otelTrace'.
- qr-preserves-behavior (`cmd/routes.go`): In deleteThing the original code calls resp.WriteHeader(http.StatusNoContent) then api.ReturnText, but with the added statusRecorder wrapping in AuthMetricsMiddleware and multiple WriteHeader-related paths this ordering is unchanged here; however the real issue is that AuthMetricsMiddleware's statusRecorder is never wired into the router, so auth.attempts is never recorded — the middleware is dead code.
- qr-not-a-noop (`cmd/saturation_metrics.go`): registerSaturationMetrics() is defined but never called anywhere (not invoked in main() or any init), so the observable gauge and its callback are never registered and http.server.worker_pool.size never emits.
- qr-preserves-behavior (`cmd/routes.go`): In getThingByID and deleteThing, the validation sub-span is created with `_, valSpan := routesTracer.Start(ctx, "validate.id")` and deferred End, but the returned child context is discarded (assigned to `_`). Subsequent metric records and the parent span operations use the outer `ctx`, so the validation span is orphaned from any nested context propagation, though it still ends correctly.
- qr-not-a-noop (`cmd/auth_metrics.go`): AuthMetricsMiddleware/statusRecorder never records auth.attempts for successful protected routes correctly because the JWTValidator middleware runs BEFORE AuthMetricsMiddleware in the chain (jwtValidator.Middleware is added first), so on a rejected request the JWT middleware writes 401 and does not call next, meaning AuthMetricsMiddleware's handler never executes and denied outcomes are never counted.
- qr-preserves-behavior (`cmd/auth_metrics.go`): The statusRecorder wraps the ResponseWriter but does not implement http.Flusher/Hijacker/other optional interfaces, so wrapping can break handlers that type-assert the ResponseWriter for streaming/hijacking, altering existing behavior for protected routes.
**Outstanding (3):**
- ⛔ qr-preserves-behavior (`cmd/routes.go`:193): In deleteThing, api.ReturnText is called after resp.WriteHeader(http.StatusNoContent). Writing a body after a 204 status header changes/breaks the existing response behavior and produces an invalid HTTP response for a 204 No Content, altering original semantics. — _Do not write a body after WriteHeader(204); either keep the original response-writing logic unchanged or remove the extra ReturnText call._
- ⚠ qr-preserves-behavior (`cmd/routes.go`:140): In getThingByID and deleteThing the validation child span (valSpan) is started but only ended in the two explicit branches; if code between Start and the branches were to panic there is no defer to end it. More importantly the span is fine here, but the pattern relies on manual End rather than defer, risking leaks if future edits add early returns. — _Use `defer valSpan.End()` immediately after Start (in a scoped closure) to guarantee the span is always ended._
- ⚠ qr-preserves-behavior (`cmd/server.go`:94): registerSaturationMetrics() and the OTel middleware run even when initOTel failed and telemetry was disabled; otelhttp.NewMiddleware and observable-gauge registration are added unconditionally, but since providers are no-op on failure this only adds overhead — acceptable, but the otelhttp middleware wraps every request adding latency to the hot path regardless of telemetry state. — _Consider gating instrumentation middleware on successful OTel init, or confirm the no-op overhead is acceptable._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`
- `cmd/routes.go`

## New files
- `cmd/otel_trace_alias.go`
- `cmd/auth_metrics.go`
- `cmd/saturation_metrics.go`

## Dependencies
- `go.opentelemetry.io/otel v1.35.0`
- `go.opentelemetry.io/otel/sdk v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.35.0`
- `go.opentelemetry.io/otel/sdk/metric v1.35.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.60.0`
