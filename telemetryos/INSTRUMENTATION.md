# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (2):**
- qr-export-pipeline (`pkg/telemetry/otel.go`): InitOTel skips both otel.SetMeterProvider and otel.SetTracerProvider whenever the already-registered global TracerProvider happens to be an *sdktrace.TracerProvider — but on a fresh process the global provider is the internal no-op delegate, so this branch is not normally taken; the risk is that if any prior init set a TracerProvider, the meter provider mp is never registered while the SDK meter provider tp is silently dropped, leaving metrics on the no-op delegate. More concretely, when this branch returns early the built mp is never registered anywhere, so all metric instruments stay bound to the no-op and emit nothing while still holding a live PeriodicReader that is never shut down beyond the returned closure.
- qr-metric-naming (`pkg/telemetry/otel.go`): The new metric names mix conventions: http.server.request.duration and http.server.requests use dotted semconv style, but auth.attempts introduces a different domain-prefix pattern and the counter carries a curly-brace unit ({request}/{attempt}) whereas semconv-style plain counts are unsuffixed/unitless — establish one consistent style.
**Outstanding (2):**
- ⚠ qr-metric-naming (`pkg/telemetry/otel.go`:48): The counter name "http.server.requests" is not an OTel semantic-convention name; the semconv name for inbound request count is folded into http.server.request.duration's count, and a plain request counter typically would be http.server.request.count or similar. Minor: acceptable dotted style but not a canonical semconv name. — _Consider http.server.request.count or rely on the histogram's count; keep dotted style consistent._
- ⚠ qr-preserves-behavior (`pkg/telemetry/otel.go`:143): chimiddleware.NewWrapResponseWriter wraps the ResponseWriter and is passed to next.ServeHTTP, which changes the concrete ResponseWriter type seen by downstream handlers; while chi's wrapper forwards common interfaces, any handler type-asserting on the original *http.response or non-forwarded optional interfaces could behave differently. — _This matches chi's own middleware pattern and is generally safe; confirm downstream handlers do not rely on unforwarded interfaces._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute

## Modified files
- `go.mod`
- `cmd/server.go`
- `pkg/auth/auth.go`

## New files
- `pkg/telemetry/otel.go`

## Dependencies
- `go.opentelemetry.io/otel v1.35.0`
- `go.opentelemetry.io/otel/metric v1.35.0`
- `go.opentelemetry.io/otel/trace v1.35.0`
- `go.opentelemetry.io/otel/sdk v1.35.0`
- `go.opentelemetry.io/otel/sdk/metric v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetricgrpc v1.35.0`
- `maintainer step: run `go mod tidy` so go.sum gains checksums for the new OTel modules (the offline verifier cannot fetch them)`
