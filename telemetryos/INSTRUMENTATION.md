# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (6):**
- qr-preserves-behavior (`cmd/routes.go`): The main() function references signal.NotifyContext, context.Background, os.Interrupt, and syscall.SIGTERM but the import block only imports errors, net/http, problem, and chi. This will fail to compile due to undefined identifiers and missing imports (context, os, os/signal, syscall).
- qr-preserves-behavior (`cmd/routes.go`): This diff defines a new func main() in cmd/routes.go, but the existing repository already has a main() (this is a routes file, not the entrypoint). Two main functions in package main is a duplicate-declaration compile error, and this replacement main() drops all the original server startup/routing logic, breaking the service.
- qr-not-a-noop (`cmd/telemetry.go`): The instruments created in telemetry.go (authAttempts, flowOutcomes, flowDuration, flowValidationOutcomes) are stored on the tel singleton but are never referenced/recorded anywhere in the diff, so no telemetry is emitted for auth or flow metrics beyond the otelhttp middleware.
- qr-preserves-behavior (`cmd/routes.go`): initOTel failure is swallowed (the error is assigned to _ and execution continues), but more critically the returned shutdown function is only deferred inside main(); if this main() is the intended entrypoint it does nothing else and exits immediately, so the server never runs and telemetry is flushed and discarded at once.
- qr-not-a-noop (`go.mod`): otel.go imports go.opentelemetry.io/otel/sdk/metric, go.opentelemetry.io/otel/sdk/trace, go.opentelemetry.io/otel/sdk/resource, go.opentelemetry.io/otel/semconv/v1.26.0, and go.opentelemetry.io/otel/propagation, but only the umbrella go.opentelemetry.io/otel/sdk module and otel are listed with no otlpmetric/otlpmetricgrpc placement issues — however go.mod is missing an explicit require for go.opentelemetry.io/contrib and the semconv/propagation packages are covered by otel. The real gap: the file imports otlpmetric/otlpmetricgrpc which IS in proposedDependencies, so builds; but there is no go.sum shown. This is not confirmable as broken.
- general (`cmd/telemetry.go`): initTelemetry() creates the auth.attempts, flow.outcomes, flow.duration, and flow.validation.outcomes instruments and stores them on tel, but no code in this diff ever records to them (only activeRequests and the otelhttp middleware emit). These instruments will exist but never emit any data points.
**Outstanding (2):**
- ⛔ qr-not-a-noop (`cmd/otel.go`:1): Under the app-sdk model this diff must register the SDK, but initOTel() and initTelemetry() are never called from main() — no main.go edit is included, so the global providers are never registered and all instruments resolve to no-ops. — _Add the main() wiring: call initOTel(ctx), defer shutdown, then initTelemetry(), and install otelMiddleware on the router._
- ⚠ qr-preserves-behavior (`cmd/middleware.go`:33): otelhttp.NewMiddleware is applied once to `next` at middleware construction time inside the outer HandlerFunc closure, but the span for the slow-request event is read from r.Context() AFTER instrumented.ServeHTTP returns; by then the otelhttp span has already ended, so AddEvent is applied to an already-ended span and is dropped. — _Record the slow_request event from within the handler chain while the span is active, or use a ResponseWriter wrapper that captures timing before the span ends._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`

## New files
- `cmd/otel.go`
- `cmd/telemetry.go`
- `cmd/middleware.go`

## Dependencies
- `go.opentelemetry.io/otel v1.35.0`
- `go.opentelemetry.io/otel/sdk v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.35.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.60.0`
