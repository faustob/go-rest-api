# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (6):**
- qr-not-a-noop (`cmd/saturation.go`): registerSaturationGauges and activeRequestsMiddleware are never called from main() or wired into the router, so the observable gauges are never registered and the active-request counter is never updated — these instruments emit nothing.
- qr-not-a-noop (`cmd/auth_telemetry.go`): authTelemetryMiddleware is defined but never registered on the router in main(), so the auth.attempts counter is never incremented and emits no telemetry.
- general (`cmd/saturation.go`): activeRequestsMiddleware references http.Handler/http.HandlerFunc but the file does not import net/http, so it will not compile as written.
- qr-not-a-noop (`cmd/routes.go`): The package-level vars routesMeter/routesTracer and the instruments in init() are created via otel.Meter()/otel.Tracer() during package initialization, which runs BEFORE main() calls initOTel(). In Go there is no delegating-proxy re-binding for instruments created before SetMeterProvider: instruments created against the default no-op provider stay no-op, so flow.outcomes/flow.duration/flow.validation.outcomes/flow.entries emit nothing at runtime.
- qr-not-a-noop (`cmd/auth_telemetry.go`): authMeter and authAttempts are created in package init() before main() calls initOTel()/SetMeterProvider, so the counter binds to the default no-op provider and auth.attempts never emits data.
- qr-preserves-behavior (`cmd/routes.go`): init() panics if any instrument creation returns an error, converting a previously non-fatal instrumentation concern into a hard startup crash of the application.
**Outstanding (1):**
- ⛔ qr-preserves-behavior (`cmd/routes.go`): In deleteThing the base already calls resp.WriteHeader(204) then ReturnText — but the added instrumentation's flowDuration.Record/flowOutcomes.Add after that are harmless. However, the statusRecorder in auth_telemetry.go wraps the ResponseWriter and its outcome logic mislabels: any handler that writes a 404 via problem.Wrap (e.g. thing-not-found) is unrelated to auth, yet the auth.attempts counter treats every non-401/403 response including 404 as outcome=success, silently miscounting auth outcomes. — _Scope authTelemetryMiddleware to only the protected route group so it records auth decisions rather than every response, and only emit auth.attempts for requests that actually pass through the JWT validator._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`
- `cmd/routes.go`

## New files
- `cmd/auth_telemetry.go`
- `cmd/saturation.go`

## Dependencies
- `go.opentelemetry.io/otel v1.35.0`
- `go.opentelemetry.io/otel/sdk v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.35.0`
- `go.opentelemetry.io/otel/sdk/metric v1.35.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.60.0`
