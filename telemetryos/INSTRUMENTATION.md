# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (6):**
- qr-not-a-noop (`cmd/otel.go`): initOTel builds and registers the SDK and populates globalInstruments, but nothing in this diff calls initOTel from main(). Under the app-sdk model this diff is responsible for wiring, so unless main() invokes initOTel the providers are never registered and globalInstruments stays nil, causing a nil-pointer panic in the middleware.
- qr-preserves-behavior (`cmd/middleware.go`): The middleware dereferences globalInstruments (inst.flowEntry.Add, inst.activeRequests.Add, etc.) with no nil check; if initOTel has not run or failed, globalInstruments is nil and every request panics, changing existing behavior from serving requests to crashing.
- qr-not-a-noop (`cmd/middleware.go`): otelhttp.NewHandler is wrapped INSIDE withOTelMiddleware, but the span-event code uses trace.SpanFromContext(ctx) with ctx = r.Context() captured BEFORE otelHandler.ServeHTTP runs; the otelhttp-created server span lives only in the request context passed to next, so SpanFromContext(ctx) here returns the non-recording parent/no-op span and span.AddEvent("slow_request") is silently dropped.
- general (`cmd/otel.go`): The proposed dependency list and go.mod require go.opentelemetry.io/otel/sdk but the code imports go.opentelemetry.io/otel/sdk/metric, go.opentelemetry.io/otel/sdk/resource, go.opentelemetry.io/otel/sdk/trace and go.opentelemetry.io/otel/semconv/v1.26.0, none of which are declared; sdk/metric in particular is a separate module (go.opentelemetry.io/otel/sdk/metric) and must be listed for the build to resolve.
- qr-preserves-behavior (`cmd/middleware.go`): otelhttp already records http.server.request.duration and creates the server span, but wrapping otelHandler in httpsnoop.CaptureMetrics re-reads the status via ResponseWriter interception; this is duplicative but more importantly the slow_request AddEvent in slowRequestHandler runs inside the otelhttp span correctly — no issue there. However the outer middleware's httpsnoop wrapping of otelHandler is redundant and does not break behavior.
- general (`cmd/main.go`): main.go references the type API (api := &API{}) and methods getThings/getThingByID/createThing/deleteThing, but no file in this diff defines the API type or these handler methods, so the package will fail to compile.
**Outstanding (1):**
- ⛔ qr-preserves-behavior (`cmd/middleware.go`:90): The statusRecorder only overrides WriteHeader; when a handler writes a body without calling WriteHeader (the common 200 path), status stays at the default 200, which is correct — but statusRecorder does not implement http.Flusher, http.Hijacker, or io.ReaderFrom, so wrapping w breaks any handler/library (e.g. chi, SSE, websockets) that type-asserts the ResponseWriter for those interfaces, changing existing behavior. — _Use a ResponseWriter wrapper that preserves optional interfaces (e.g. chi's middleware.NewWrapResponseWriter or httpsnoop) instead of a bare embedded struct._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`

## New files
- `cmd/otel.go`
- `cmd/middleware.go`
- `cmd/main.go`

## Dependencies
- `go.opentelemetry.io/otel v1.33.0`
- `go.opentelemetry.io/otel/sdk v1.33.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.33.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.33.0`
- `go.opentelemetry.io/otel/metric v1.33.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.58.0`
- `go.opentelemetry.io/otel/sdk/metric v1.33.0`
- `go.opentelemetry.io/otel/semconv/v1.26.0 v1.26.0`
