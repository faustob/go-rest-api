# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (7):**
- qr-not-a-noop (`pkg/telemetry/otel.go`): SetupOTelSDK builds and registers a TracerProvider but never creates or registers a MeterProvider via otel.SetMeterProvider. All the metric instruments in metrics.go bind to the global default no-op meter, so none of the HTTP/auth/flow metrics are ever exported.
- qr-not-a-noop (`pkg/telemetry/middleware.go`): The call httpRequestDuration.Record(req.Context(), duration.Seconds(), func() metricOptOrNil() { return nil }()...) is not valid Go — metricOptOrNil is a struct type alias, not a []metric.RecordOption, so the variadic spread does not compile.
- qr-not-a-noop (`pkg/telemetry/middleware.go`): authAttempts.Add(req.Context(), 1, func() metricOptOrNil() { return nil }()...) uses the bogus metricOptOrNil type alias as a variadic option slice, which does not compile.
- qr-not-a-noop (`pkg/telemetry/middleware.go`): The recorded http.server.request.duration histogram and auth.attempts counter are called with no attributes (the option slice is always nil), so the carefully built attrs (route, method, status) and auth outcome/reason are never attached to the emitted metrics.
- qr-preserves-behavior (`pkg/telemetry/middleware.go`): statusRecorder wraps the ResponseWriter but implements only Flush, not http.Hijacker or io.ReaderFrom; the comment claims Hijack/ReadFrom passthroughs are preserved but they are not, so WebSocket upgrades / hijacking on wrapped routes will break where the underlying writer supported them.
- qr-preserves-behavior (`cmd/server.go`): The final content registers telemetry.AuthOutcomeMiddleware BEFORE jwtValidator.Middleware on the protected router. Since chi runs middleware in registration order, AuthOutcomeMiddleware's next.ServeHTTP wraps the validator, so it inspects the response status after the validator runs — but as ordered, AuthOutcome runs outermost and the validator inner, which is the intended order per the comment; however the two conflicting hunks (one adding auth after validator, one reordering it before) indicate the emitted order places AuthOutcome first, contradicting its own documented requirement that it run AFTER the validator to observe the 401/403 outcome.
- qr-preserves-behavior (`pkg/telemetry/middleware.go`): AuthOutcomeMiddleware wraps the ResponseWriter in a statusRecorder that does not implement http.Pusher and only conditionally forwards Flush/Hijack/ReadFrom; passing this wrapped writer down to the JWT validator and downstream handlers changes the concrete ResponseWriter type for all protected routes, which can break type assertions in downstream handlers.
**Outstanding (1):**
- ⛔ qr-preserves-behavior (`pkg/telemetry/middleware.go`:95): AuthOutcomeMiddleware is registered AFTER jwtValidator.Middleware in the protected group. Chi runs middleware in registration order, so the JWT validator runs and writes its 401/403 before AuthOutcomeMiddleware's handler executes; because the validator short-circuits and never calls next() on denial, AuthOutcomeMiddleware's next.ServeHTTP is never invoked for denied requests, so denied auth attempts are never counted — the middleware only ever records 'allowed'. — _Register AuthOutcomeMiddleware BEFORE jwtValidator.Middleware so its statusRecorder wraps the validator and captures the 401/403 status it writes, then inspect rec.status after next returns._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `cmd/server.go`
- `go.mod`

## New files
- `pkg/telemetry/otel.go`
- `pkg/telemetry/metrics.go`
- `pkg/telemetry/middleware.go`

## Dependencies
- `go.opentelemetry.io/otel v1.36.0`
- `go.opentelemetry.io/otel/sdk v1.36.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.36.0`
- `go.opentelemetry.io/otel/metric v1.36.0`
- `go.opentelemetry.io/otel/trace v1.36.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.36.0`
