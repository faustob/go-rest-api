# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 1 generation pass — ⚠ passed with warnings.
**Outstanding (4):**
- ⚠ qr-endpoint-env-driven (`pkg/telemetry/telemetry.go`:82): Hardcoded OTLP endpoint "localhost:4317" — wrong in every real deployment. — _Read the endpoint from OTEL_EXPORTER_OTLP_ENDPOINT / config instead of a literal._
- ⚠ qr-not-a-noop (`cmd/routes.go`:30): Route handlers use telemetry.Tracer.Start() but Tracer is only assigned inside InitSDK; if InitSDK fails (endpoint unreachable), main() continues without telemetry and Tracer remains nil, causing a nil-pointer panic on the first request to any instrumented handler. — _Initialise Tracer to otel.Tracer(scopeName) at package init (the global proxy is safe as a no-op before registration) instead of only inside InitSDK, so it is never nil._
- ⚠ qr-not-a-noop (`cmd/routes.go`:26): getThings/getThingByID/createThing/deleteThing call telemetry.FlowEntryCounter.Add / TenantRequestCounter.Add etc.; these package-level instruments are only assigned inside InitSDK's initInstruments(), so if InitSDK fails they remain nil and the handlers panic with a nil-pointer dereference on the first request. — _Guard each instrument use with a nil check (as done in auth.go) or initialise instruments unconditionally against the global proxy meter at package init._
- ⚠ qr-not-a-noop (`cmd/server.go`:48): RegisterSaturationCallback is invoked unconditionally in main() even when InitSDK failed; it calls otel.Meter(scopeName) which resolves to the global no-op provider, but the observable gauges then never emit while the code claims saturation monitoring is active. — _Only call RegisterSaturationCallback when InitSDK succeeded (inside the else branch), matching the guarded instrument pattern._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`
- `pkg/auth/auth.go`
- `cmd/routes.go`

## New files
- `pkg/telemetry/telemetry.go`

## Dependencies
- `github.com/riandyrn/otelchi v0.12.1`
- `go.opentelemetry.io/otel v1.35.0`
- `go.opentelemetry.io/otel/trace v1.35.0`
- `go.opentelemetry.io/otel/metric v1.35.0`
- `go.opentelemetry.io/otel/sdk v1.35.0`
- `go.opentelemetry.io/otel/sdk/metric v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.35.0`
- `go.opentelemetry.io/otel/semconv/v1.26.0 (transitively via go.opentelemetry.io/otel/sdk)`
- `google.golang.org/grpc v1.71.1`
