# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 2 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (4):**
- qr-export-pipeline (`cmd/otel.go`): The MeterProvider is built with `sdkmetric.NewManualReader()` and nothing in the process ever calls `metricReader.Collect(...)` (there is no Prometheus bridge or scrape handler attached to it), so every metric recorded by cmd/telemetry.go — the availability, error-rate, throughput, p95/p99 latency and auth-failure instruments — accumulates in memory and never leaves the process. All six metric-backed SLIs would have no data.
- general (`cmd/telemetry.go`): `statusRecorder.ReadFrom` references `io.Reader`, `io.ReaderFrom` and `io.Copy`, but the `io` package is not in the import block of cmd/telemetry.go, so the file does not compile as written.
- qr-metric-naming (`cmd/telemetry.go`): The new counter is named `http.server.request.total` with unit `{request}`; per the semconv style adopted for the other new names in this diff, plain counts are unsuffixed (no `.total`), so this name mixes a Prometheus-style total suffix into an otherwise semconv-shaped namespace.
- general (`cmd/telemetry.go`): The custom histogram is named `http.server.request.duration`, the same name otelhttp (registered one middleware earlier in cmd/server.go) emits for inbound request duration from its own instrumentation scope, producing two same-named series with different attribute sets for the same measurement and ambiguous/duplicated latency data for the p95/p99 SLIs.
**Outstanding (3):**
- ⚠ qr-metric-naming (`cmd/telemetry.go`:57): The new latency histogram is named "http.server.route.duration" although it measures exactly the inbound HTTP server request duration in seconds, which OTel semconv already names http.server.request.duration; the otelhttp middleware added in cmd/server.go also records a server duration histogram, so the same measurement lands under two non-semconv-aligned names and backs the p95/p99 SLIs from a non-standard stream. — _Rename the histogram to "http.server.request.duration" (unit "s") so the p95/p99 SLI queries use the semantic-convention stream, and rely on a single duration source rather than recording request latency twice._
- ⚠ general (`cmd/server.go`:76): For a chi router the wiring recipe prescribes otelchi.Middleware, but this diff wraps the chain with otelhttp.NewHandler(next, "http.server"); otelhttp has no knowledge of chi route templates, so every server span is named the constant "http.server" and its server metrics carry no http.route attribute, defeating the metrics→exemplar-trace per-handler pivot the SLIs call for. — _Replace the otelhttp.NewHandler wrapper with router.Use(otelchi.Middleware(serviceName, otelchi.WithChiRoutes(router))) so spans and metrics carry the chi route pattern while the variable stays a chi.Router._
- ⚠ general (`go.mod`:18): The stated OTel target for this service is 1.45, but all OTel modules are pinned to v1.35.0 (with contrib otelhttp v0.60.0), contradicting the authoritative target version. — _Pin go.opentelemetry.io/otel, otel/metric, otel/trace, otel/sdk, otel/sdk/metric and the OTLP exporters to the 1.45 line and the matching contrib release, and use the semconv package version shipped with that SDK._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute

## Modified files
- `go.mod`
- `cmd/server.go`
- `cmd/routes.go`

## New files
- `cmd/otel.go`
- `cmd/telemetry.go`

## Dependencies
- `go.opentelemetry.io/otel v1.35.0`
- `go.opentelemetry.io/otel/metric v1.35.0`
- `go.opentelemetry.io/otel/trace v1.35.0`
- `go.opentelemetry.io/otel/sdk v1.35.0`
- `go.opentelemetry.io/otel/sdk/metric v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.35.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.60.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.35.0`
