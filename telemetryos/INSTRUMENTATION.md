# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (7):**
- qr-not-a-noop (`cmd/telemetry.go`): newThingAPITelemetry() creates all the custom instruments but is never called anywhere in the diff (main() never invokes it and api.tel is never assigned). Every route handler guards its metric emissions with `if api.tel != nil`, so api.tel is always nil and none of the custom counters/histograms/gauge ever emit.
- qr-not-a-noop (`cmd/routes_protected.go`): The handlers use metric.WithAttributes(...) but the file's import block only imports otel, attribute, and trace — the metric package is never imported, so all flow counter/histogram Add/Record calls reference an undeclared symbol and the custom flow telemetry cannot function as written.
- qr-not-a-noop (`cmd/middleware.go`): activeRequestMiddleware is defined but never registered on the router in cmd/server.go, so the activeRequestCount that the observable gauge callback reads is never incremented/decremented and the slow-request span events are never produced.
- qr-preserves-behavior (`cmd/server.go`): The base repo's addPublicRoutes/addProtectedRoutes are being redefined in the new cmd/routes_protected.go; if the original ThingAPI already defined these methods this introduces duplicate method declarations. Also the ThingAPI struct is referenced with a new `tel` field that is not shown to be added to the struct definition.
- general (`cmd/routes_protected.go`): cmd/routes_protected.go declares new addProtectedRoutes and addPublicRoutes methods on ThingAPI, but cmd/api.go already defines those same methods on ThingAPI. This duplicate method declaration in the same package makes the build fail, and even if disambiguated the two definitions conflict on which routes/handlers are registered.
- qr-preserves-behavior (`cmd/routes_protected.go`): The instrumented wrappers always record flow.outcomes with outcome="success" and flow.duration unconditionally after calling the underlying handler, regardless of whether the handler wrote a 4xx/5xx or errored. This mislabels every request as a success, producing incorrect SLI telemetry.
- qr-preserves-behavior (`cmd/routes_protected.go`): createThing was previously only registered as a public route (r.Post("/things", api.createThing) in api.go's addPublicRoutes) but the new addProtectedRoutes registers POST /things as protected and addPublicRoutes no longer registers it, changing the auth requirements and route wiring of the existing API.
**Outstanding (2):**
- ⚠ qr-not-a-noop (`cmd/telemetry.go`:123): authAttempts is declared, created, and stored on the struct but never incremented anywhere in the diff, so the auth-failure-rate SLI counter emits no data. — _Call api.tel.authAttempts.Add(...) at the actual auth decision site (e.g. in the JWT validator middleware) with the appropriate outcome/deny.reason attributes, or remove the unused instrument._
- ⛔ qr-preserves-behavior (`cmd/routes_protected.go`:56): statusRecorder only overrides WriteHeader, not Flush/Hijack/Push/ReaderFrom, so wrapping the underlying ResponseWriter strips those optional interfaces from the handlers; any downstream code relying on http.Flusher/Hijacker will lose that capability, changing existing behavior. — _Preserve the original interfaces (e.g. use httpsnoop.CaptureMetrics or implement the optional interfaces via delegation), or capture status without wrapping the ResponseWriter._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`
- `cmd/api.go`

## New files
- `cmd/telemetry.go`
- `cmd/middleware.go`
- `cmd/routes_protected.go`

## Dependencies
- `go.opentelemetry.io/otel v1.35.0`
- `go.opentelemetry.io/otel/sdk v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.35.0`
- `go.opentelemetry.io/otel/sdk/metric v1.35.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.60.0`
