# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (8):**
- qr-endpoint-env-driven (`cmd/server.go`): Hardcoded OTLP endpoint "http://localhost:4317" — wrong in every real deployment.
- general (`cmd/server.go`): The import block references go.opentelemetry.io/otel/semconv/v1.26.0 but the semconv package is provided by go.opentelemetry.io/otel, and there is no such versioned subpackage path guaranteed at v1.33.0; more critically the imports are not sorted/grouped and mix sdk/metric, sdk/trace, sdk/metric/metricdata without the sdk/metric dependency being declared in go.mod. sdkmetric and metricdata come from go.opentelemetry.io/otel/sdk which is present, so that is fine, but semconv v1.26.0 must be verified to exist; the real blocker is the missing go.opentelemetry.io/otel/sdk/metric usage without issue — see next finding.
- qr-preserves-behavior (`cmd/routes.go`): In getThingByID the not-found branch records flow outcome/validation and sends the 404 but the enclosing span is never given a successful status only on the error path; more importantly there is no behavior regression here — omit. The real issue: activeRequests is incremented via RecordFlowEntry at the start of every handler but if the handler panics before RecordFlowOutcome (e.g. inside ReturnJSON), the UpDownCounter is never decremented, leaking active-request count. The Recoverer middleware catches the panic but the deferred span.End() runs while the counter decrement does not.
- qr-not-a-noop (`cmd/server.go`): If initOTel fails, main() logs a warning and continues without registering any MeterProvider/TracerProvider, and meter.Int64Counter etc. are still created on the no-op global provider, so all custom metrics and spans silently emit nothing while the service runs normally. Since telemetryCollection is 'none' and there is no fallback exporter, a common startup failure (no OTLP endpoint reachable) results in zero telemetry with only a warning.
- edit-not-applied (`cmd/thing_api.go`): An intended edit to cmd/thing_api.go could not be applied (its target text wasn't found verbatim, wasn't unique, or overlapped another edit), so the file was left unchanged to avoid a half-applied result: Define ThingAPI struct with OTel instrument fields, NewThingAPI constructor, SetOTelInstruments setter, and RecordFlowEntry/RecordFlowOutcome/RecordValidationOutcome methods with pointer receivers.
- qr-not-a-noop (`cmd/routes.go`): routes.go calls api.RecordFlowEntry, api.RecordFlowOutcome, and api.RecordValidationOutcome, and server.go calls api.SetOTelInstruments, but none of these methods (nor the ThingAPI struct fields storing the instruments) are defined in the diff; the code will not compile.
- general (`cmd/routes.go`): getThings, getThingByID, createThing, and deleteThing are defined with a value receiver (api ThingAPI) but routes_register.go registers them via a pointer receiver (api *ThingAPI), and server.go uses api := NewThingAPI(); if NewThingAPI returns a value while methods are split across value/pointer receivers with mutable instrument fields, the SetOTelInstruments state set on a pointer will not be visible on value-receiver handler copies.
- qr-not-a-noop (`cmd/server.go`): initOTel uses otlptracegrpc.New/otlpmetricgrpc.New with no endpoint configured and telemetryCollection is 'none', so with no OTLP collector reachable the exporters' batch/periodic export will silently fail and no telemetry will be delivered.
**Outstanding (4):**
- ⚠ qr-endpoint-env-driven (`cmd/server.go`:63): Hardcoded OTLP endpoint "localhost:4317" — wrong in every real deployment. — _Read the endpoint from OTEL_EXPORTER_OTLP_ENDPOINT / config instead of a literal._
- ⛔ general (`cmd/server.go`:34): The import block declares the standard-library and OTel imports in an order that violates gofmt but, more importantly, imports "go.opentelemetry.io/otel/sdk/metric/metricdata" — but go.mod does not list go.opentelemetry.io/otel/sdk/metric as a required module dependency and the proposedDependencies omit it too, though it is transitively part of the sdk module. This is not a compile blocker. — _n/a_
- ⚠ qr-preserves-behavior (`cmd/routes.go`:54): getThingByID and deleteThing call RecordFlowOutcome (which decrements activeRequests and records duration) but omit the RecordFlowEntry increment path only on the success branch where they do call it; however both branches call RecordFlowEntry and RecordFlowOutcome so counter balance is preserved — no issue. — _n/a_
- ⚠ qr-not-a-noop (`cmd/server.go`:86): initOTel constructs the OTLP gRPC connection with grpc.NewClient and creates exporters, but telemetryCollection is 'none' meaning no collector/endpoint exists; the periodic reader and batch processor will repeatedly fail to export. This does not break request handling but the emitted telemetry never reaches a backend. — _Confirm an OTLP endpoint/collector is deployed in the docker environment, or the metrics/traces will be produced but silently dropped on export failure._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`
- `cmd/routes.go`

## New files
- `cmd/routes_register.go`
- `cmd/thing_api.go`
- `cmd/thing_api_middleware.go`

## Dependencies
- `go.opentelemetry.io/otel v1.33.0`
- `go.opentelemetry.io/otel/sdk v1.33.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.33.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.33.0`
- `go.opentelemetry.io/otel/metric v1.33.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.58.0`
- `google.golang.org/grpc`
