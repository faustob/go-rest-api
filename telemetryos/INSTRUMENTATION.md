# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (8):**
- qr-not-a-noop (`cmd/routes.go`): In every handler the span.AddEvent slow-request event is created with otelTrace.WithAttributes(...), but WithAttributes returns a trace.EventOption (a variadic option), not the string event name AddEvent expects. The event is passed as the option list only if the first arg is a name; here the intended event name argument is missing so the emitted span event is malformed/misconfigured, and the p99-budget event never carries a proper name.
- qr-not-a-noop (`cmd/routes.go`): In recordFlowRequest the slow-request span event calls span.AddEvent("p99-budget-exceeded", otel.Tracer("go-rest-api/flow")): the second argument is a Tracer, not a trace.EventOption, so this does not compile and, if repaired, emits no meaningful attributes. The helper itself is also never invoked anywhere in the diff, so its instrumentation is dead code.
- qr-preserves-behavior (`cmd/routes.go`): In deleteThing the added flowDuration.Record/flowOutcomes.Add/flowValidationOutcomes.Add calls execute after resp.WriteHeader(http.StatusNoContent) and api.ReturnText, which itself is a pre-existing quirk; but the instrumentation additionally records flow metrics after ReturnJSON/ReturnText has written the body, changing nothing in output. This is acceptable and omitted — flagging instead: the createThing/getThings success-path always records outcome "success" even though nothing distinguishes failures, producing metrics that misreport outcomes.
- qr-not-a-noop (`cmd/telemetry.go`): The observable gauge registration runs inside a goroutine spawned from init(), which executes at package load before main() calls initOTel(); the meter/RegisterCallback resolve against the global MeterProvider proxy so this is not fatal, but ActiveRequestMiddleware and RecordAuthOutcome are never wired into the router in the diff, so the active-request gauge and auth counter never receive updates from real requests.
- qr-not-a-noop (`cmd/telemetry.go`): The init() function launches a goroutine that registers observable gauges by calling otel.Meter(...) and RegisterCallback at package-init time, but main()'s initOTel() (which registers the SDK MeterProvider) runs later. If the goroutine executes before SetMeterProvider, the gauges bind to the delegating global meter which does resolve later, but the RegisterCallback race means these saturation gauges may silently emit nothing.
- qr-not-a-noop (`cmd/telemetry.go`): RecordAuthOutcome is defined to record auth.attempts but is never invoked anywhere in the diff (the JWT middleware is not modified to call it), so the auth.attempts counter emits no telemetry.
- qr-not-a-noop (`cmd/routes.go`): recordFlowRequest is defined with span, counters and histogram recording but is never called by any route handler; each handler instead inlines its own flow metric recording, so recordFlowRequest is dead code that emits nothing.
- qr-preserves-behavior (`cmd/routes.go`): In deleteThing the instrumentation calls flowDuration.Record/flowOutcomes.Add after resp.WriteHeader(http.StatusNoContent) and api.ReturnText; this is a pre-existing quirk (writing a body after 204) but the added metric calls execute after the response write, which is acceptable, however the span/metric recording happens post-write which is harmless. Only real concern: repeated success outcomes double-counted with flow.outcomes fired on every handler even when otelhttp already tracks duration.
**Outstanding (2):**
- ⚠ qr-not-a-noop (`cmd/routes.go`): The unused RecordAuthOutcome function and its auth.attempts counter are never wired into the JWT middleware, so the authAttempts counter never records any authentication decision. — _Call RecordAuthOutcome from the JWT validator middleware on allow/deny paths, or remove the dead instrumentation._
- ⚠ general (`cmd/routes_otel_imports.go`): This new file only declares 'package main' with a comment; it serves no purpose and references a non-existent otelTrace alias in its comment, adding a redundant empty file to the package. — _Delete cmd/routes_otel_imports.go entirely._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`
- `cmd/routes.go`

## New files
- `cmd/telemetry.go`
- `cmd/routes_otel_imports.go`

## Dependencies
- `go.opentelemetry.io/otel v1.35.0`
- `go.opentelemetry.io/otel/sdk v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.35.0`
- `go.opentelemetry.io/otel/sdk/metric v1.35.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.60.0`
- `go.opentelemetry.io/otel/trace v1.35.0`
