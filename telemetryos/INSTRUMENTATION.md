# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (7):**
- general (`cmd/server.go`): The handler wrapping references an undefined identifier `goodtelhttp.NewHandler`, but the import alias is `gootelhttp`. This is a compile error (undefined: goodtelhttp).
- qr-not-a-noop (`cmd/server.go`): initTelemetry()/initAppMetrics() is never called in main(), so globalMetrics stays nil and every custom instrument (auth.attempts, flow.*, active_requests) is skipped by the nil guards in the middleware — none of the custom metrics ever emit.
- qr-not-a-noop (`cmd/middleware.go`): flowTelemetryMiddleware and authTelemetryMiddleware are defined but never registered via router.Use(...) in server.go, so none of the flow/auth/saturation metrics are ever recorded at runtime.
- qr-preserves-behavior (`cmd/middleware.go`): statusRecorder wraps the ResponseWriter but does not implement http.Flusher/Hijacker/etc.; inserting it between otelhttp and chi strips those optional interfaces from downstream handlers, breaking streaming/websocket/SSE handlers that type-assert on the ResponseWriter.
- general (`cmd/server.go`): otelhttp.NewHandler(router, serviceName) is created and passed to StartServer, but this is done AFTER initOTel/initTelemetry (which is fine); however the deferred shutdownOtel() is registered inside main via `defer shutdownOtel()`, and StartServer blocks until the server stops, so shutdown works. The real problem: the deferred shutdown is only registered in the else-branch success path — acceptable. Omit.
- qr-preserves-behavior (`cmd/middleware.go`): The custom statusRecorder wrapping in flowTelemetryMiddleware replaces the ResponseWriter for downstream handlers; while it forwards Flush/Hijack/ReadFrom, its ReadFrom uses a non-standard signature (interface{ Read([]byte)(int,error) }) that does NOT match io.ReaderFrom, so http will never detect it as an io.ReaderFrom and this method is dead code, but it does not break behavior.
- qr-preserves-behavior (`cmd/auth_telemetry.go`): authTelemetryMiddleware wraps the response in a statusRecorder before the JWT validator runs; if the validator relies on the ResponseWriter implementing http.Flusher/Hijacker without those being detected on the wrapper, streaming could break — but statusRecorder does forward those interfaces, so this is acceptable. Omit.
**Outstanding (3):**
- ⚠ qr-endpoint-env-driven (`cmd/otel.go`:4): Hardcoded OTLP endpoint "localhost:4317" — wrong in every real deployment. — _Read the endpoint from OTEL_EXPORTER_OTLP_ENDPOINT / config instead of a literal._
- ⚠ qr-preserves-behavior (`cmd/middleware.go`:115): ReadFrom's EOF detection compares err.Error()=="EOF" instead of errors.Is(err, io.EOF); a non-standard reader wrapping io.EOF (e.g. via fmt.Errorf %w) would not match and would be returned as an error instead of terminating the copy, corrupting large streamed responses when the underlying writer lacks io.ReaderFrom. — _Use errors.Is(err, io.EOF) to detect end of stream, or use io.Copy for the fallback path._
- ⚠ qr-preserves-behavior (`cmd/auth_telemetry.go`:25): authTelemetryMiddleware wraps the ResponseWriter in a statusRecorder that does not implement http.CloseNotifier and only conditionally forwards Flusher/Hijacker; when the request has no Authorization header it passes the original writer through, but for authenticated requests the JWT validator downstream loses any interface not covered by statusRecorder, though the covered set (Flusher/Hijacker/ReaderFrom) is complete for typical handlers. — _Confirm no downstream handler relies on http.CloseNotifier or http.Pusher on protected routes, or forward those interfaces too._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`

## New files
- `cmd/otel.go`
- `cmd/telemetry.go`
- `cmd/middleware.go`
- `cmd/auth_telemetry.go`
- `cmd/otel_init_hook.go`

## Dependencies
- `go.opentelemetry.io/otel v1.35.0`
- `go.opentelemetry.io/otel/sdk v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.35.0`
- `go.opentelemetry.io/otel/sdk/metric v1.35.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.60.0`
