# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (6):**
- qr-not-a-noop (`cmd/telemetry.go`): The package-level meter is created via otel.Meter("go-rest-api") and all instruments are built in init(), which runs before main() calls setupOTelSDK(). Go's otel.Meter returns a delegating meter, but the observable gauges and their RegisterCallback are captured at init time; the counters/histograms defined here (auth.attempts, flow.outcomes, flow.entries, flow.duration, flow.validation.outcomes) are never incremented/recorded anywhere in the diff, so those five custom instruments emit no data at runtime.
- qr-not-a-noop (`cmd/telemetry.go`): The active_requests observable gauge callback always reports the package-level activeRequests variable, which is never incremented or decremented anywhere in the diff, so it perpetually reports 0 and conveys no in-flight request information.
- general (`cmd/otel.go`): The MeterProvider is created with metric.NewMeterProvider(metric.WithResource(res)) but no metric reader/exporter is configured, so no periodic reader collects the registered instruments and metrics are never exported anywhere.
- general (`cmd/otel.go`): The "go.opentelemetry.io/otel/sdk/resource" package is imported and used but is not declared in go.mod's require block (only the umbrella .../sdk module is listed as a direct dependency); resource is part of the sdk module so this compiles, but time import is only kept alive by a discard assignment which is dead code.
- qr-not-a-noop (`cmd/telemetry.go`): The package-level meter is created via otel.Meter("go-rest-api") at package-variable init time, and the init() function creates all instruments and registers the observable-gauge callback, but package init runs BEFORE main() calls setupOTelSDK(). At init time no MeterProvider is registered, so otel.Meter returns the global default which is a no-op until SetMeterProvider is called; the created instruments and callback bind to the no-op provider and never emit.
- qr-not-a-noop (`cmd/telemetry.go`): recordAuthAttempt uses authAttemptsCounter but no code path in the diff calls recordAuthAttempt, so the auth.attempts counter is never incremented and emits nothing.
**Outstanding (1):**
- ⚠ qr-preserves-behavior (`cmd/telemetry.go`:48): recordAuthAttempt is defined but never called anywhere in the diff (authOutcomeMiddleware calls it, but only the counter path — recordAuthAttempt is invoked). Actually it is used; disregard. — _n/a_

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`
- `cmd/routes.go`

## New files
- `cmd/otel.go`
- `cmd/telemetry.go`
- `cmd/telemetry_middleware.go`

## Dependencies
- `go.opentelemetry.io/otel v1.36.0`
- `go.opentelemetry.io/otel/sdk v1.36.0`
- `go.opentelemetry.io/otel/sdk/metric v1.36.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.36.0`
- `go.opentelemetry.io/otel/semconv/v1.26.0 v1.26.0`
- `go.opentelemetry.io/contrib/instrumentation/github.com/go-chi/chi/v5/otelchi v0.61.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.36.0`
