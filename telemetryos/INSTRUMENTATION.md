# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (5):**
- qr-unwired-telemetry (`pkg/telemetry/middleware.go`): The auth decision counter only records when hasCredentials(r) is true, but the chi RoutePattern read in the deferred function is read on the ProtectedRouter's JWT middleware which short-circuits denied requests. However the deeper problem is that HTTPTelemetryMiddleware is registered on the top-level router BEFORE the protected Group, so it does wrap auth — but the route pattern will be 'unmatched' for auth-rejected requests since the group's route context is not populated on 401/403 short-circuit. This makes the auth.route label meaningless for denials.
- qr-metric-naming (`pkg/telemetry/instruments.go`): The new metric name 'http.server.request.count' mixes the dot-separated semconv style with a '.count' suffix on a plain counter; per semconv style plain counts are unsuffixed (the semconv name is http.server.request or a total-style count without '.count').
- qr-metric-naming (`pkg/telemetry/instruments.go`): The new metric name 'auth.decisions.total' uses a '.total' suffix (Prometheus-style) which is inconsistent with the dot-separated semconv style used elsewhere in this file where plain counts are unsuffixed.
- qr-export-pipeline (`pkg/telemetry/otel.go`): The MeterProvider is built with sdkmetric.NewMeterProvider(WithResource(res)) and NO reader/exporter attached. Every metric instrument (serverDuration, requestsTotal, authAttempts, requestsActive) records into a provider with no reader, so no metric telemetry can ever leave the process.
- qr-metric-naming (`pkg/telemetry/instruments.go`): New counter name "http.server.requests" and "http.server.active_requests" deviate from OTel semconv, which defines http.server.active_requests but a request count is not a standard semconv name; more importantly the repo mixes dotted names throughout which is consistent, but "auth.attempts" introduces a domain outside http with no service prefix — acceptable, but "http.server.requests" as a plain count is fine only if it does not collide with semconv duration naming.
**Outstanding (1):**
- ⚠ qr-metric-naming (`pkg/telemetry/instruments.go`:38): The new counter name "http.server.request.count" adds a "count" suffix to a plain counter, which violates the semconv style (plain counts are unsuffixed); it should be "http.server.request" or similar unsuffixed name.

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute

## Modified files
- `go.mod`
- `cmd/server.go`

## New files
- `pkg/telemetry/otel.go`
- `pkg/telemetry/instruments.go`
- `pkg/telemetry/middleware.go`

## Dependencies
- `go.opentelemetry.io/otel v1.35.0`
- `go.opentelemetry.io/otel/metric v1.35.0`
- `go.opentelemetry.io/otel/trace v1.35.0`
- `go.opentelemetry.io/otel/sdk v1.35.0`
- `go.opentelemetry.io/otel/sdk/metric v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.35.0`
- `github.com/riandyrn/otelchi v0.12.1`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.35.0`
