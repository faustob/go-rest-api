# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (6):**
- general (`cmd/server.go`): The middleware closure references http.Handler and http.HandlerFunc via the net/http package, but net/http is not imported in cmd/server.go. This will fail to compile.
- qr-preserves-behavior (`cmd/server.go`): The diff's hunks reorder the protected route middleware so that telemetry.AuthOutcomeMiddleware runs BEFORE jwtValidator.Middleware. AuthOutcomeMiddleware wraps next and inspects the status after downstream runs, but by inserting it ahead of the validator it changes middleware ordering relative to the original code where only jwtValidator.Middleware existed; the intended semantics (auth observed after validation) are actually preserved since it wraps next, but the ordering churn across contradictory hunks is ambiguous.
- qr-preserves-behavior (`pkg/telemetry/instruments.go`): statusRecordingResponseWriter wraps the ResponseWriter but does not forge the optional http.Flusher, http.Hijacker, or io.ReaderFrom interfaces despite the type comment claiming it forwards all optional interfaces; handlers that type-assert for Flusher (e.g. streaming/SSE) will silently lose flushing behavior after this middleware wraps the writer.
- qr-preserves-behavior (`cmd/server.go`): AuthOutcomeMiddleware and the outer otelhttp/RequestOutcomeMiddleware wrap the ResponseWriter in statusRecordingResponseWriter, which only forwards WriteHeader/Write and hides optional interfaces (http.Flusher, http.Hijacker, io.ReaderFrom) implemented by the underlying writer. This changes existing behavior for handlers relying on those interfaces (e.g. streaming/flushing) and can break them.
- qr-preserves-behavior (`pkg/telemetry/instruments.go`): The high-cardinality label http.route is set to req.URL.Path (the raw request path, including IDs and arbitrary segments) rather than a route template, producing unbounded metric cardinality that can overwhelm the metrics pipeline.
- general (`pkg/telemetry/instruments.go`): instrumentsMeter is assigned in initInstruments but only read in RegisterSaturationCallbacks; the flowEntryCounter/flowOutcomeCounter add duplicate coverage but that is intentional — no compile issue, noting the unused-looking start/rctx dead code (start measured then discarded, rctx checked then discarded) as dead instrumentation.
**Outstanding (3):**
- ⚠ general (`cmd/server.go`:58): The middleware wrapper uses http.Handler / otelhttp.NewHandler but the file's import block does not import "net/http". This will fail to compile. — _Add "net/http" to the import block in cmd/server.go._
- ⛔ qr-preserves-behavior (`pkg/telemetry/instruments.go`:180): AuthOutcomeMiddleware is registered via protectedRouter.Use AFTER jwtValidator.Middleware, so the JWT validator runs FIRST and short-circuits (writing 401/403) before this middleware's ServeHTTP is even invoked; the auth-outcome counter therefore never observes denied requests and effectively only records allowed ones, making the SLI wrong. — _Register AuthOutcomeMiddleware BEFORE jwtValidator.Middleware (protectedRouter.Use(AuthOutcomeMiddleware) then Use(jwtValidator.Middleware)) so it wraps the validator and can observe the rejection status._
- ⛔ qr-not-a-noop (`pkg/telemetry/instruments.go`:100): RegisterSaturationCallbacks obtains the meter via MeterProvider().Meter(...) creating a NEW meter distinct from the one used to create activeRequestsGauge/workerPoolSizeGauge in initInstruments (mp.Meter within SetupOTelSDK); RegisterCallback on a meter for instruments not created by that same meter is undefined/rejected by the SDK, so the saturation gauges may never be observed. — _Store the meter created in initInstruments in a package variable and reuse it for RegisterCallback, or move the callback registration into initInstruments using the same meter instance._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `cmd/server.go`

## New files
- `pkg/telemetry/otel.go`
- `pkg/telemetry/instruments.go`

## Dependencies
- `go.opentelemetry.io/otel v1.36.0`
- `go.opentelemetry.io/otel/sdk v1.36.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.36.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.36.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.61.0`
- `go.opentelemetry.io/otel/metric v1.36.0`
- `go.opentelemetry.io/otel/trace v1.36.0`
