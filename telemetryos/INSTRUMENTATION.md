# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (6):**
- qr-not-a-noop (`cmd/telemetry.go`): setupOTelSDK never builds or registers a MeterProvider — it only creates and registers a TracerProvider via otel.SetTracerProvider. All the custom metric instruments (counters, histograms, observable gauges) therefore bind to the default no-op MeterProvider and emit nothing.
- qr-not-a-noop (`cmd/telemetry.go`): The package-level var meter = otel.Meter(...) is evaluated at package initialization before setupOTelSDK runs, and since no MeterProvider is ever registered it permanently resolves to the no-op meter; even the observable-gauge callbacks produce no output.
- qr-preserves-behavior (`cmd/server.go`): telemetryMiddleware is registered before api.SimpleCORSMiddleware and before the JWT auth middleware but its comment claims it runs before auth to observe auth rejections; because it wraps next.ServeHTTP and reads chi RoutePattern after the handler, on auth-rejected requests the route pattern is populated but the added metric recording adds per-request work on every request including OPTIONS/CORS preflight. This is additive-only and does not change control flow.
- qr-preserves-behavior (`cmd/telemetry.go`): statusCapturingWriter only forwards the http.Flusher interface; it does not implement http.Hijacker or io.ReaderFrom, so wrapping the writer silently drops those interfaces for any downstream handler (e.g. WebSocket upgrades) that type-asserts on the original ResponseWriter.
- qr-preserves-behavior (`cmd/telemetry.go`): The telemetryMiddleware is registered before api.SimpleCORSMiddleware and the JWT auth middleware, and it reads chi.RouteContext(r.Context()).RoutePattern() AFTER next.ServeHTTP. Its comment claims it runs before auth so it observes auth rejections, but this is instrumentation-caused ordering; more importantly the middleware is not a behavior problem here — omit. (Keeping only real issues below.)
- qr-preserves-behavior (`cmd/telemetry.go`): telemetryMiddleware wraps the ResponseWriter in statusCapturingWriter and passes it to downstream handlers, but otelchi.Middleware is registered BEFORE telemetryMiddleware, so otelchi wraps first; the deleteThing handler calls resp.WriteHeader(204) then ReturnText which writes a body — this is pre-existing app behavior and out of scope, so it is omitted. Removing this placeholder.
**Outstanding (1):**
- ⛔ qr-preserves-behavior (`cmd/telemetry.go`:274): recordValidationOutcome calls tracer.Start(ctx, ...) but discards the returned context (uses `_`), so the created span is not propagated and, more importantly, the span is started and immediately ended on every call — but the discarded child context means any nested work is never parented. Combined with the discarded context this creates a detached, empty span for each validation step rather than proper span nesting.

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`
- `cmd/routes.go`

## New files
- `cmd/telemetry.go`

## Dependencies
- `go.opentelemetry.io/otel v1.36.0`
- `go.opentelemetry.io/otel/sdk v1.36.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.36.0`
- `go.opentelemetry.io/otel/metric v1.36.0`
- `go.opentelemetry.io/otel/trace v1.36.0`
- `go.opentelemetry.io/contrib/instrumentation/github.com/go-chi/chi/otelchi v0.61.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.36.0`
