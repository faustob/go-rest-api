# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (7):**
- qr-not-a-noop (`cmd/auth_middleware.go`): authTelemetryMiddleware is registered as a global chi middleware (r.Use), so it runs on every request. It calls authAttemptsCounter.Add, but that counter is only initialized inside initInstruments() which runs only if initOTel succeeds. If OTel init fails (main.go only logs a warning and continues), authAttemptsCounter is nil and Add panics on every request.
- qr-preserves-behavior (`cmd/main.go`): withFlowInstrumentation calls flowEntryCounter.Add / flowValidationOutcomesCounter.Add / flowOutcomesCounter.Add / flowDurationHistogram.Record on every route. If initOTel fails (main.go only logs a warning and continues without registering instruments), all these instrument variables are nil and the handlers panic instead of serving requests, changing existing behavior.
- qr-preserves-behavior (`cmd/main.go`): span.SetAttributes is called on the span from r.Context() in withFlowInstrumentation without checking span.IsRecording(); if OTel init failed and no provider is registered this is a no-op span so it is safe, but the recorded flow.validation.outcome is derived only from header presence and does not reflect the actual auth decision made downstream, mislabeling requests with invalid/expired tokens as 'passed'.
- qr-preserves-behavior (`cmd/main.go`): withFlowInstrumentation wraps the ResponseWriter in a statusRecorder before calling the real handler h(rw, r). This changes the concrete ResponseWriter type seen by the original handlers, potentially altering behavior for handlers that type-assert for interfaces beyond those forwarded, and it double-wraps since p99BudgetMiddleware already wraps in a statusRecorder.
- edit-not-applied (`cmd/main.go`): An intended edit to cmd/main.go could not be applied (its target text wasn't found verbatim, wasn't unique, or overlapped another edit), so the file was left unchanged to avoid a half-applied result: Wire authTelemetryMiddleware into the router so auth.attempts counter is recorded.
- edit-not-applied (`cmd/main.go`): An intended edit to cmd/main.go could not be applied (its target text wasn't found verbatim, wasn't unique, or overlapped another edit), so the file was left unchanged to avoid a half-applied result: Remove duplicate statusRecorder type definition from main.go to avoid redeclaration (it is defined in middleware.go).
- edit-not-applied (`cmd/main.go`): An intended edit to cmd/main.go could not be applied (its target text wasn't found verbatim, wasn't unique, or overlapped another edit), so the file was left unchanged to avoid a half-applied result: Remove the duplicate statusRecorder struct and methods from main.go; the canonical definition lives in middleware.go.
**Outstanding (4):**
- ⛔ general (`cmd/auth_middleware.go`:47): jwksURIConfigured() contains an `import "os"` statement inside the function body and a closure, which is a syntax error — Go import declarations may only appear at the top of a file. This file will not compile. — _Add `"os"` to the file's top-level import block and replace the body with `return os.Getenv("JWKS_URI") != ""`._
- ⚠ qr-not-a-noop (`cmd/auth_middleware.go`:19): authTelemetryMiddleware and its authAttemptsCounter recording are never wired into the router in main.go (only activeRequestMiddleware and p99BudgetMiddleware are registered), so the auth.attempts counter is never emitted at runtime. — _Add `r.Use(authTelemetryMiddleware)` to the router setup in main.go._
- ⚠ qr-preserves-behavior (`cmd/middleware.go`:73): statusRecorder.Hijack has signature `Hijack() (interface{}, interface{}, error)` which does not match the standard `http.Hijacker` interface `Hijack() (net.Conn, *bufio.ReadWriter, error)`; wrapping the ResponseWriter therefore silently strips the real Hijacker interface, breaking WebSocket/connection upgrades that previously worked. — _Implement Hijack with the correct signature `Hijack() (net.Conn, *bufio.ReadWriter, error)` and type-assert against `http.Hijacker`._
- ⚠ qr-preserves-behavior (`cmd/main.go`:100): withFlowInstrumentation wraps the response writer in statusRecorder before calling the real handler, which strips the concrete http.ResponseWriter's optional interfaces (only Flush/Hijack are forwarded, and Hijack has the wrong signature), potentially breaking streaming/upgrade behavior of the original handlers. — _Ensure statusRecorder correctly forwards all optional interfaces (http.Hijacker with correct signature, http.Flusher, io.ReaderFrom, http.Pusher) or avoid wrapping when not needed._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`

## New files
- `cmd/otel.go`
- `cmd/telemetry.go`
- `cmd/middleware.go`
- `cmd/main.go`
- `cmd/auth_middleware.go`
- `cmd/helpers.go`

## Dependencies
- `go.opentelemetry.io/otel v1.33.0`
- `go.opentelemetry.io/otel/sdk v1.33.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.33.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.33.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.58.0`
