# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (9):**
- general (`cmd/otel.go`): gomaxprocsValue() is defined but never called anywhere in the diff. In Go this produces an "declared and not used" style dead code, but more importantly it references import_runtime_once and runtimeGOMAXPROCS to justify a helper that has no callers, adding a compile-clean but purposeless function. This is not a compile error by itself, but the entire runtime helper wiring is unused.
- general (`go.mod`): The require block adds direct OTel dependencies but omits transitive requirements (e.g. go.opentelemetry.io/otel/metric, go.opentelemetry.io/otel/trace, go.opentelemetry.io/proto/otlp, google.golang.org/grpc, go.opentelemetry.io/auto/sdk) that the imported packages need; the go.sum and indirect require entries are not updated, so the module will not build without `go mod tidy`.
- qr-preserves-behavior (`cmd/server.go`): otelhttp.NewHandler wraps the entire chi router including the /metrics and /health endpoints that the original filtered request logger deliberately excludes, and it creates a server span for every request; this changes behavior on the previously-untraced metrics/health hot paths and can double-instrument. Additionally the server span uses a static operation name "serviceName" ("change-me") rather than a per-route name.
- qr-preserves-behavior (`cmd/server.go`): initOTel is called after all routes are registered but before StartServer; if initOTel fails it only logs and continues, but on success the otelhttp handler still wraps the router even though tracing may partially initialize — the ordering is acceptable, however the deferred shutdownOtel is registered inside an else branch such that any subsequent panic before StartServer would still run it, which is fine, but there is no signal handling so shutdown never runs on normal SIGTERM in Docker, meaning batched spans/metrics are lost on shutdown.
- general (`cmd/otel.go`): otel.go imports "os" but never uses it after the semconv/env logic — actually os.Getenv is used, so os is used. The unused import concern is the "metric" package: it is used. The real compile error is gomaxprocsValue is defined but never called, and it references import_runtime_once and runtimeGOMAXPROCS creating dead but compilable code — however Go does not error on unused functions.
- general (`go.mod`): go.mod adds go.opentelemetry.io/otel/sdk/metric, otelhttp, otlpmetricgrpc, and semconv/v1.26.0 as direct requires but omits required transitive dependencies (e.g. go.opentelemetry.io/otel/metric, go.opentelemetry.io/otel/trace, google.golang.org/grpc, go.opentelemetry.io/proto/otlp, go.opentelemetry.io/otel/sdk/metric's deps) from the require blocks, so the module will not build without a go mod tidy that the diff does not reflect.
- qr-preserves-behavior (`cmd/server.go`): The OTel SDK is initialized via initOTel(context.Background()) and its shutdown is deferred in main(), but StartServer blocks until the server stops; this is fine — the issue is the periodic metric reader and batch span processor are only flushed on graceful shutdown, and there is no signal handling wired, so a container SIGTERM (docker stop) kills the process without running the deferred shutdownOtel(), dropping buffered telemetry. This is pre-existing behavior but the metrics/traces added here will silently not export on normal container termination.
- general (`cmd/server.go`): gootelhttp.NewHandler wraps the router with serviceName which is the literal "change-me", so the produced http.server span operation label will be "change-me" rather than a meaningful operation name.
- qr-not-a-noop (`cmd/otel_middleware.go`): RecordAuthAttempt, RecordFlowOutcome, and RecordValidationOutcome are defined but never called anywhere in the diff, so auth.attempts, flow.outcomes, flow.duration, and flow.validation.outcomes instruments are created but never record any measurements and emit no data at runtime.
**Outstanding (5):**
- ⚠ qr-endpoint-env-driven (`cmd/otel.go`:7): Hardcoded OTLP endpoint "localhost:4317" — wrong in every real deployment. — _Read the endpoint from OTEL_EXPORTER_OTLP_ENDPOINT / config instead of a literal._
- ⛔ general (`cmd/server.go`:76): The code calls `goodtelhttp.NewHandler(...)` but the imported package alias is `gootelhttp`. This undefined identifier is a compile error. — _Use the correct alias: `gootelhttp.NewHandler(router, serviceName)`._
- ⛔ general (`cmd/otel.go`:21): The `attribute` package is imported in otel.go but never used in that file, which is a compile error in Go (imported and not used). — _Remove the unused `go.opentelemetry.io/otel/attribute` import from cmd/otel.go._
- ⚠ general (`cmd/otel_middleware.go`:1): activeRequestMiddleware is defined but never wired into the router in server.go, so activeRequestCount is never incremented/decremented and the http.server.active_requests gauge always reports 0. — _Register activeRequestMiddleware via router.Use(...) or wrap the handler so in-flight requests are actually counted._
- ⚠ general (`cmd/otel.go`:150): The custom metric http.server.worker_pool.size exposes GOMAXPROCS, which is not a worker-pool size; net/http uses goroutine-per-connection with no fixed pool, so this metric is misleading as a saturation SLI. — _Remove the worker_pool.size gauge or expose a real concurrency limit metric instead._

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`
- `cmd/routes.go`

## New files
- `cmd/otel.go`
- `cmd/otel_runtime.go`
- `cmd/otel_middleware.go`

## Dependencies
- `go.opentelemetry.io/otel v1.35.0`
- `go.opentelemetry.io/otel/sdk v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.35.0`
- `go.opentelemetry.io/otel/sdk/metric v1.35.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.60.0`
- `go.opentelemetry.io/otel/semconv/v1.26.0 v1.35.0`
