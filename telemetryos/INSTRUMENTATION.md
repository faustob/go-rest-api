# Instrumentation for Go-rest-api

Generated by TelemetryOS against `094fa38`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** go · go-modules · host: docker · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (5):**
- qr-not-a-noop (`cmd/routes.go`): Both the existing getThings handler and the new instrumentedHandler wrapper (registered via addPublicRoutesOTel/addProtectedRoutesOTel) instrument the same routes. The /things route is registered twice under the same chi router (once via addPublicRoutes -> getThings, once via addPublicRoutesOTel -> instrumentedHandler(getThings)), so getThings emits flow.entries/flow.duration/flow.outcomes/validation spans AND the wrapper emits the same metrics for the same request, producing duplicated telemetry.
- qr-preserves-behavior (`cmd/routes.go`): The same route pattern (e.g. GET /things and GET /things/{id}) is registered twice on the chi router — once by addPublicRoutes and again by addPublicRoutesOTel. go-chi panics at registration time when the same method+pattern is mounted twice, crashing the server on startup.
- qr-not-a-noop (`cmd/routes.go`): The existing getThings handler is instrumented directly (spans, flow.entries/duration/outcomes, activeRequests) AND is also registered a second time via addPublicRoutesOTel, whose instrumentedHandler wraps getThings and emits the identical flow.entries/duration/outcomes/validation metrics and spans again. Every /things GET therefore double-counts all flow metrics and produces duplicated/nested spans.
- qr-preserves-behavior (`cmd/server.go`): Both addPublicRoutes and addPublicRoutesOTel register the same paths (GET /things, GET /things/{id}), and addProtectedRoutes/addProtectedRoutesOTel likewise register POST /things and DELETE /things/{id}. chi panics at startup on duplicate route registration for the same method+pattern, crashing the server.
- qr-preserves-behavior (`cmd/routes_otel.go`): RecordAuthOutcome and authAttemptCounter are defined but never called anywhere in the diff, so the auth.attempts counter never emits any data.
**Outstanding (4):**
- ⛔ qr-preserves-behavior (`cmd/routes.go`:95): getThings calls span.AddEvent("slow_request", otel.Tracer(...).Start) passing a method value as the event option, immediately followed by a second AddEvent that references an undefined `trace` package. This is broken/duplicated telemetry code that will not produce a valid span event and is placeholder garbage.
- ⚠ general (`cmd/routes.go`:97): The `trace` package is referenced (trace.WithAttributes) in getThings but is not imported in routes.go, so the intended span event cannot be emitted as written.
- ⚠ general (`cmd/routes_otel.go`): instrumentedHandler, addPublicRoutesOTel, addProtectedRoutesOTel and RecordAuthOutcome are defined but never wired into the router (server.go still calls api.addProtectedRoutes/addPublicRoutes), so this file's flow-level instrumentation is dead code and emits nothing at runtime.
- ⚠ qr-not-a-noop (`cmd/routes.go`:50): activeRequests is incremented/decremented and read in the observable-gauge callback without any synchronization; under concurrent requests this is a data race on the shared int64 and will yield corrupted saturation readings (and may trigger the race detector/crash).

## SLIs implemented
- **Go-rest-api — HTTP Service Availability** (`go-rest-api-http-availability`) — target 99.95% over 30-day
- **Go-rest-api — HTTP Response Time P95** (`go-rest-api-http-latency-p95`) — target < 200ms over 1-hour
- **Go-rest-api — HTTP Response Time P99** (`go-rest-api-http-latency-p99`) — target < 750ms over 1-hour
- **Go-rest-api — HTTP 5xx Error Rate** (`go-rest-api-http-error-rate`) — target < 0.1% over 1-hour
- **Go-rest-api — HTTP Worker Pool Saturation** (`go-rest-api-http-saturation`) — target < 80% over 5-minute
- **Go-rest-api — Authentication Failure Rate** (`go-rest-api-http-auth-failure-rate`) — target < 2% over 1-hour
- **Go-rest-api — HTTP Request Throughput** (`go-rest-api-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `go.mod`
- `cmd/server.go`
- `cmd/routes.go`

## New files
- `cmd/routes_otel.go`

## Dependencies
- `go.opentelemetry.io/otel v1.35.0`
- `go.opentelemetry.io/otel/sdk v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc v1.35.0`
- `go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc v1.35.0`
- `go.opentelemetry.io/otel/sdk/metric v1.35.0`
- `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp v0.60.0`
